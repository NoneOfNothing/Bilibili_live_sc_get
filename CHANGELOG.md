# 更新日志（Changelog）

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，
版本号采用语义化版本（语义上以 MINOR 位承载功能新增）。

## [未发布]

### 新增
- **快捷弹幕支持「点选即发送」（ROADMAP 92，Tk / Qt 两版及各自独立窗口）**：发送行内新增「**点选即发送**」勾选框——勾选后点选快捷弹幕**直接发出**，不再只填入输入框；不勾选则维持原行为（填入、可先改后发）。关键约束：**直接发送复用同一条发送链路**（把三处发送入口的主体抽成 `_send_danmaku_text` / `send_danmaku_text`，点选与手动发送都经由它），因此写操作门控、登录态、发送冷却、回复目标、颜色与模式**完全一致**，不会因为多出一个入口而绕过任何限制；发送时**不改动输入框内容**（用户写了一半的内容不会被覆盖）。开关是全局偏好 `ui.quick_dm_send_now`（默认关闭），三处视图（Tk 主界面 / Tk 房间独立窗口 / Qt 弹幕面板，独立窗口复用同一面板）由**宿主统一入口** `set_quick_dm_send_now` 落盘并同步——Qt 广播给每个 `DmPanel`（`blockSignals` 防回环）、Tk 回写主界面与各独立窗口，切换记一条 info 日志。实测（假宿主直调未绑定方法）：未勾选点选 → 只填入「打卡」且未发送、下拉复位；勾选后点选 → 已发送 `['打卡']` 且输入框里「用户已输入的半截内容」保持不变；统一入口切换 → `ui.quick_dm_send_now` 落盘、主界面勾选框回写、独立窗口同步 `[False, True]`。新增 4 项测试（三处勾选框齐备、点选按开关分支且复用统一入口、手动与点选都经由同一方法、两版统一入口落盘并广播）。顺带补上 README 里一直缺失的快捷弹幕说明（ROADMAP 90 那轮只写了 CHANGELOG）。
- **快捷弹幕（ROADMAP 90，按房间独立，Tk / Qt 两版及各自独立窗口）**：为每个直播间维护一组常用弹幕文本，发送行内多出一个下拉（**不新增行高**），**点选后只把文本填入输入框、不直接发送**（可先改后发，避免误触）；主界面另有「管理」按钮，弹出对话框增删 / 上移下移，**关闭即保存**（独立窗口只作使用、不提供管理入口，避免多窗口同时编辑同一份数据）。**按房间独立**存储于 `data/gui_rooms.json` 房间条目的 `quick_danmaku`（`RoomEntry` 新增字段，`save_room_entries` 经 `asdict` 自动带上），读入由纯函数 `normalize_quick_danmaku` 清洗（只留非空字符串 / 去首尾空白 / 超长按 20 字截断 / 保序去重 / 上限 12 条 / 坏数据不抛异常），旧配置缺该键自动为空、无需迁移。填入规则抽成两版共用纯函数 `merge_quick_danmaku`：输入框为空直接填入、非空则**追加**（不覆盖用户已输入的内容）、已恰好相同则不重复叠加。交互细节：Tk 用 `ttk.Combobox` 的 `postcommand` 在每次展开前刷新候选 + `<<ComboboxSelected>>`，Qt 用 `QComboBox.activated`（重复选同一项也会触发）；房间内无预设时下拉置灰（明确表示「这个房间还没配过」）；下拉跟随当前房间（`_refresh_dm_send_state` 里统一刷新），Qt 侧管理对话框关闭后遍历 `_dm_panels` 刷新主界面与所有独立窗口的下拉。实测（Tk 启动 + 自建房间）：`('打卡','好耶')` / `readonly` 与空列表 / `disabled` 两种状态正确；空输入框点选得 `打卡`、非空点选得 `打卡 好耶` 且字数同步；管理对话框添加「新快捷语」、删除首条后关闭，落盘为 `['好耶','新快捷语']`。新增 16 项离线测试（`tests/test_quick_danmaku.py`：清洗规则 7 项、填入规则 5 项、按房间持久化 4 项；该文件刻意不 import qt_app / PySide6，以便无 Qt 环境也能执行）。
- **SC 面板头部显示「已播」直播时长（Tk / Qt 两版，ROADMAP 86）**：醒目留言头部信息行新增一段 `已播 01:23:45`（排在「舰长」之后），**每秒跳动**；**只在房间处于「直播中」且已拿到起点时才出现**——未开播 / 轮播中 / 刚启动还没数据时，那一行与没有该功能时**完全一致**（用户明确要求）。**起点口径**：优先用接口给的**真实开播时间**（程序在主播开播中途启动也能显示准确时长），接口取不到时回退为「本程序第一次看到它处于直播中」的本地观测时刻。**实测（2026-09-22，本地 17 个直播间）两种来源**：`room/v1/Room/get_info` → `live_time` 是**北京时间字符串**（直播中 `"2026-09-22 16:35:40"`、未开播 `"0000-00-00 00:00:00"`）；H5 `getH5InfoByRoom` → `room_info.live_start_time` 是**秒级时间戳**（直播中 `1790066140`、未开播与轮播均为 `0`），两者换算完全吻合。**H5 时间戳优先**（无时区歧义，且 `get_full_room_info` 本来就在调它，零额外请求），`get_info` 字符串作回退（解析时**显式按 UTC+8**，否则非东八区环境算出的时长会整体偏 8 小时）；另一候选 `getRoomInfoOld` 实测返回 `code=-400`、不可用故弃用。取值一律要求「非零、非占位、不超过本地时钟 2 分钟」——异常值返回 `None` 交给回退，**绝不在解析层编造时间**（否则时长会凭空开始计时）。实现分三层：① `api.parse_live_started_at`（纯函数）归一化成 `live_started_at`（epoch 秒）写回房间信息 dict；② `client._emit_status` 把它放进 status 事件（**非直播中自动清空**），两版只读查询路径（被占用 / 已停用监听的房间）的 `room_info` 事件也带上它，两条来源共用同一个键；③ 两版宿主用共享纯函数 `update_live_started_at` 维护起点——**接口值优先并可校正**先到的本地观测（开播推送常早于接口刷新），**同状态重复上报不漂移**（周期状态复核会反复上报同一状态，若每次都重记，起点会被一路推后、时长永远从零开始），下播即清空；渲染用共享纯函数 `format_live_duration` / `live_duration_text`（固定两位补零、超 24 小时按累计小时、负数钳到 0）。**每秒跳动复用现有 100ms 轮询做 1 秒节流**（Qt 在 `ScPanel.poll_tick`、Tk 在 `_poll_queue`），不新增定时器；只刷「正在直播且有起点」的当前房间与各房间独立窗口，其余房间一次 `setText` 都不做。**「已播」起点与排序用的先后标记（`_live_since`，值是自增整数）完全分离**，互不影响（含回归测试锁住）。离屏实测：直播间头部 → `房间 7 · 雨纪_Ameki · 标题：… · 同接 157 · 舰长 213 · 已播 01:42:29`；未开播 / 直播中但起点未知 → 该字段整段消失；「累计 SC」标签不受影响。
- **房间独立子窗口（ROADMAP 84，Tk / Qt 两版）**：房间列表**右键 → 「打开独立窗口」**即可单开一个只显示该直播间 **SC 区 + 弹幕区**的窗口（不含房间列表 / 粉丝牌 / 调试页 / 工具栏），用于同时盯多个直播间而不用在主界面来回切换。两区能力与主程序相同：SC 的实时渲染、历史分页（滚到顶继续加载 + 「已读取共 x 条」标记）、累计计数、吸底与「N 条新 SC」徽标、点用户名跳个人空间、退款/删除 SC 置灰、头部（房间·主播·标题·同接·舰长·粉丝牌）；弹幕的实时显示、点击正文复制、点用户名跳转、右键回复/@、表情面板与点选发送、发送弹幕（含颜色/模式/回复）、表情悬浮看原图、未读徽标与 4000 行裁剪保护。**一房一窗、可同时开多个**：重复对同一房间右键只把已有窗口前置，窗口各自绑定自己的房间——主界面切到别的房间也不会影响它。窗口行为：**按房间记忆位置与尺寸**（写 `data/gui_rooms.json` 的 `ui.room_windows`，移动/缩放去抖 1 秒落盘 + 关窗立即落盘；恢复时做屏幕范围校验，避免窗口落在已拔掉的显示器上）、打开时**不抢主窗口焦点**（`WA_ShowWithoutActivating` / `overlay.apply_noactivate`）、**不置顶**、**主窗口关闭时统一回收**、**房间被删除时自动关闭**。实现上把「当前房间」从宿主全局态改为**视图级**：Qt 版抽出 `qt_sc_panel.ScPanel` 组件与 `qt_room_window.RoomChatWindow`，宿主按 room_id 分发事件（SC/删除/弹幕/表情/发送结果/历史，历史结果按 token 回投发起面板），`DmPanel` 的当前房间改为注入式（主视图跟随选中、窗口固定房间），独立窗口的弹幕区恒显示并计入弹幕接收门控；Tk 版新增 `tk_room_window.RoomChatWindow(tk.Toplevel)`（自带渲染与历史读盘队列，**不消费宿主的 `ui_queue`**），由宿主在事件分发处按房间广播。写操作（发弹幕/表情）仍受 `config.json` 的 `allow_write_operations` 与登录态门控，不新增旁路。**Qt 窗口底部也带「弹幕表情图」勾选框**（与主界面底部是同一个全局开关，统一走 `qt_app.set_dm_emoticon_image`）：任一处切换都会把主界面与**所有**独立窗口的勾选框、以及每个弹幕面板的内嵌图状态一起同步（`blockSignals` 防回环），偏好仍落 `ui.dm_emoticon_image`。顺带修掉 Qt 版「删除/退款 SC 置灰」在 PySide6 下静默失效的问题（`QTextBlock.textLayout()` 未绑定，调用即 `AttributeError` 且被槽函数吞掉，改用块长度判空块）。
- **直播预览 · P4 打磨（加密房间密码 / 清晰度记忆 / 失败提示 / 收口）**：① **加密（密码）直播间**：拉流前先问 `getRoomPlayInfo` 的 `encrypted` / `pwd_verified`（新增 `api.get_stream_info` 与纯函数 `parse_room_play_info`）——**加密且明确未通过密码时不再白拉一次流**，直接把「该房间是加密直播间」回投界面；格子提示引导后，**右键格子 → 「输入密码…」**弹出密码框（`QInputDialog`，密码模式），提交时先调 `api.verify_room_pwd`（社区接口、官方文档未收录，失败不致命），再**带 `pwd` 重拉**（`get_stream_info(pwd=...)` 与 `get_live_stream_urls(pwd=...)` 都会带上，两条路一起走命中率最高）。密码**只存在这一路的内存里**：切房 / 停止预览 / 退出程序即丢弃，不写入任何配置文件（界面提示与 README 都写明）。② **清晰度记忆**：在浮窗里改过的清晰度写回 `data/gui_rooms.json` 的 `ui.preview_quality`，下次启动沿用（此前只在本次运行内有效，重启又回到 `config.json` 的默认值）。③ **失败提示文案**：新增纯函数 `describe_preview_error`，把错误码 / 异常翻成一句可操作的中文（未开播 → 「开播后点刷新即可」、加密未解锁 → 「右键该格输入密码…」、密码错 → 「密码未通过」、`-101 / -352 / -403 / -412` → 登录或风控、网络异常 → 检查网络），取不到具体原因时也不再甩一串裸 code。④ 文档与测试收口：新增 13 项测试（加密状态透传、带密码请求、密码校验、文案翻译、密码只存内存、清晰度记忆接线等）。
- **直播预览 · P3 多路宫格（最多 4 路）**：预览浮窗从「单路」升级为**多路宫格**——`PreviewTile`（一路：自己的 `QMediaPlayer` / `QAudioOutput` / `QVideoWidget` 与**独立回环代理**）由 `PreviewWindow` 统一编排（布局、主路、声音、资源保护）。**加入方式**：Ctrl 多选后点工具栏「预览」批量加入（按钮文案随「选中的房间是否都已在预览」在「预览 / 停止预览」间切换，再点即停掉这些路）、房间行右键「预览该房间 / 停止预览该房间 / 设为主路」；浮窗内每格右下「✕」停掉该路。**布局**按路数自动排宫格（1 路全屏、2 路左右分栏、3~4 路 2×2，`grid_shape` 纯函数）。**主路**：点某一格（或右键「设为主路」）把它设为**有声音**的那一路，其余强制静音（避免多路声音混在一起），主路边框高亮、标题栏显示主路房间；静音 / 音量 / 暂停 / 刷新都作用于主路，清晰度是全局值（各房间按其可用档位落到实处，格子标题显示该路**实际**档位）。**上限**：`preview.max_rooms`（默认 4，1~4，硬上限 4），到达上限再加入会被拒绝并提示。**资源保护**：副路卡顿只自动重连 1 次（`HEALTH_MAX_RELOADS_SUB`），仍不行就**停掉该路**（主路仍走原断流自愈：3 次上限 + 提示手动刷新）；另有 **CPU 守护**——每 30 秒采样进程 CPU，连续两次超过 70%（且不止一路）时自动停掉**最后加入的副路**（`pick_victim_index` 纯函数）。顺手优化：每路的可用清晰度只在首次拉流时问一次并缓存（此前每次换源都会多打一次接口）；观看时长上报（P2）改为**只对主路上报**，切主路即按新房间重新累计；「跟随选中房间」语义收敛为——选中已在预览中的房间→设为主路，只有一路时→把这一路换成它，多路且不在其中→不动作（否则点几下列表就满了）。
- **直播预览 · P2 观看时长上报（写操作，默认关闭）**：预览浮窗新增「**上报观看时长**」开关——开启后，预览播放期间按服务端节奏（约每 1 分钟）向 B 站上报一次网页端观看心跳（`GET https://live-trace.bilibili.com/xlive/rdata-interface/v1/heartbeat/webHeartBeat`，参数只有 `hb` 与 `pf=web`，其中 `hb = base64("<next_interval>|<真实房间号>|1|0")`），并在开关旁**显示本次已上报的时长**（如「已上报 12:00」）。三重安全约束：① **默认关闭**（`config.json` 的 `preview.watch_time`，配置说明里写明风险）；② 还要求 `allow_write_operations = true`（写操作总开关）与**已登录**（Cookie），任一不满足时勾选会被拒绝，并把原因写在开关旁（「需先把 config.json 的 allow_write_operations 改为 true」/「需先登录（点主界面『获取Cookie』）」）；③ **只在真正在看时上报**——预览进行中、未暂停、主播在播、播放器正在播放四者同时满足才发一次，暂停 / 缓冲 / 断流 / 未开播都不计（判定抽成纯函数 `needs_watch_report`）；暂停或切房后**不补报**，切房即按新房间重新累计。失败（未登录 / 网络 / 风控）**立即停表并提示**，不自动重试（避免风控与无谓请求）；服务端返回的间隔夹到 **15~300 秒**再使用，异常值回退 60 秒。接口层新增 `api.report_watch_heartbeat`（其中 `heartbeat_hb` / `parse_next_interval` 为纯函数，便于离线测试；未登录直接抛 `ApiError`，**不发请求**）。注意：这是**模拟网页端行为**的高风险写操作，可能触发风控，请自行评估后再开启。
- **直播预览断流自愈（ROADMAP 63 · P4 的第一块）**：直播流会**静默卡死**——连接被服务端断开或直链过期时，`QMediaPlayer` 可能停在 `StoppedState` 却**不触发** `errorOccurred`，画面定格在最后一帧，此前只能手动点「刷新」。现加入**每 5 秒一次的健康检查**（`HEALTH_CHECK_MS`）：满足「预览进行中 + 用户未暂停 + 窗口可见 + 主播在播 + 播放器未在播放」即自动重新拉流换源，状态栏与日志显示「播放中断，正在自动重连（第 N 次）…」。三条工程约束：① **刚装载完有 15 秒宽限期**（`HEALTH_GRACE_S`）——换档 / 换源 / 起播缓冲期间播放器可能尚未进入 `PlayingState`，没有宽限期就会把正常起播误判成断流、反复重载反而永远起不来；宽限期同时充当两次自动重连之间的最小间隔；② **连续重试上限 3 次**（`HEALTH_MAX_AUTO_RELOADS`），用尽后只提示一次「播放已中断，自动重连未成功——请点『刷新』重试」并停手（死链重试到天亮也不会活过来，只会在状态栏与日志里刷垃圾），播放**恢复正常即把重试预算清零**，手动「刷新」也重新给预算；③ **用户主动暂停、窗口不可见、主播未开播都不算断流**（未开播时流本来就不存在；主界面直播状态取不到时按「在播」处理，宁可多试一次也别漏自愈）。判定抽成纯函数 `needs_auto_reload`（7 项条件，10 项离线测试覆盖）。做法与参考项目 DD_Monitor 一致（其 `VideoWidget_vlc.py` 在定时器里 `not is_playing() and liveStatus != 0 and not userPause → mediaReload()`），我们另补了宽限期与重试上限。实测（假实例直调 `_on_health_tick`）：连续 6 次心跳恰好重连 3 次后停手并只提示一次；播放恢复后预算归零；暂停 + 未开播时 0 次重连；直播状态未知时仍会重连（不漏自愈）。
- **直播预览（Qt 版专属，ROADMAP 63 · P1）**：主界面工具栏新增「**预览**」按钮，在**独立浮窗**里播放所选直播间的直播流；房间列表**右键房间行**同样可以「预览该房间 / 停止预览」（菜单标题显示房间号与主播名，右键非选中房间时先切流再选中它，避免与「跟随选中房间」互相打架）——浮窗可缩放 / 置顶 / 拖到任意位置，内部有播放·停止、清晰度切换、静音与音量、状态提示。技术链路：`api.get_live_stream_urls` 带 Cookie 拉直链（`platform=web`→http-flv、`platform=h5`→hls）→ `live_preview.LiveStreamProxy` 在 **127.0.0.1 随机端口**起回环代理，代注入 `Referer`/`Origin`/`Cookie`（HLS 还要把 m3u8 里的分片地址改写到代理，否则播放器直连分片照样 403）→ `QMediaPlayer` 只连本机地址播放。**HLS 优先**：播放列表由代理逐次转发，直链过期只需 `set_streams` 换源、播放器无需重连；直链到期前每 **10 分钟**自动换源，播放出错自动换另一种格式（HLS ⇄ FLV）重试一次。**未登录只能看 720P**（B 站对匿名请求的清晰度限制）：未登录时清晰度下拉只保留 360P / 720P 两档，并提示「点『获取Cookie』登录后可看更高清晰度」，登录后自动放开全部档位。默认**静音**（避免切房突然出声）、默认置顶、默认**跟随**主界面选中的直播间；默认值写在 `config.json` 的 `preview` 段（`quality` / `mute` / `volume` / `always_on_top` / `follow_room`）。Tk 版没有可用的视频组件，本轮不做（按双版方针只保留框架无关的拉流与代理层）。**「暂停」与「停止预览」是两件事**：浮窗内的按钮改为**暂停 / 继续**——暂停只是 `QMediaPlayer.pause()`（当前画面定格，**预览仍在进行、代理不释放**，期间还可以换清晰度），继续时若流已断开（暂停久了直链过期/连接被断）会自动重新拉流；而工具栏「停止预览」、右键「停止预览」与直接关窗都会停止播放、释放本地代理并**关闭浮窗**（程序退出同样收窗；`closeEvent` 内显式传 `close=False`，避免「关闭 → 停止 → 再关闭」递归）。浮窗另加**「刷新」**按钮：直播流没有「seek 到直播点」的通用手段，暂停后继续只会从暂停处接着播、反复暂停会越来越滞后，点「刷新」即重新拉流跳到最新画面；**暂停超过 30 秒再继续时也会自动刷新**（短暂停仍直接续播，避免每次重连）。**默认清晰度是「自动」**（取该房间可用最高档，`preview.quality` 默认 `0`）；清晰度下拉只列**该房间真正可用**的档位——旧接口 `playUrl` 的 `accept_quality` 返回的其实是清晰度**编号**（实测 `['4']`），拿它当 qn 过滤会把 4K/杜比等拉不到的档位也列出来，现改用新接口 `getRoomPlayInfo` 的 `accept_qn`（实测该房间为 `[10000, 400, 250]`），且名称与 B 站一致、不带分辨率（同一档位在不同房间的分辨率并不固定；旧写法「超清 1080P」这类标注会误导）；未登录则只有 360P / 720P，选中不可用档位会**自动回退到可用最高档并提示**后重新拉流；**切换清晰度会自动重新装载**——本地代理地址是固定的，直接 `setSource` 同一个 URL 不会重新加载（表现就是黑屏），因此换档/换格式时先 `setSource(QUrl())` 清空再设新地址。
- **粉丝牌等级与任务（只读）**：新增「粉丝牌」页签，集中展示账号持有的全部粉丝牌（勋章名/等级/主播/房间/今日亲密度/点亮与直播状态）与各监听房间的每日任务进度（观看直播满15分钟、发弹幕、点赞等，「当前/上限」与是否完成）；任务上限**随粉丝牌等级提升、每日刷新**，一律以接口返回为准（不写死）。醒目留言标题栏同步显示当前房间的粉丝牌等级。需先用「获取Cookie」取得登录态 Cookie。
- **按房间完成粉丝牌写任务（手动 / 自动）**：直播间列表提供「**发弹幕（选中房间）**」「**点赞（选中房间）**」两个**独立按钮**（顺序与任务列表列顺序一致），各自只执行对应任务且**仅作用于该房间**（不连带执行另一项、不影响其它直播间）；也可按房间分别勾选「自动发弹幕」/「自动点赞」，程序约每 5 分钟补做未完成任务。发弹幕默认**轮次发送该直播间专属表情包**，无专属表情包时回退**纯数字文本**（`1`、`2`、`3`……递增）。**完成即停**（接口标记完成或进度达上限即停止），按房间串行并随机间隔节流（默认点赞 15~20s、弹幕 6~8s），点赞仅在直播间开播时执行；连续失败达上限停止本轮，命中风控立即中止、不做重试风暴。
- 新增应用配置 `medal_tasks`（`auto` 全自动总开关、`like_interval_sec`、`danmaku_interval_sec`、`max_retry`），**默认全部关闭**。自动执行需 `allow_write_operations` + `medal_tasks.auto` + 该房间已勾选三者同时满足，任一不满足只提示不执行。
- **开播信号直接触发自动点赞**：直播间**开播瞬间**即自动执行该房间的「自动点赞」任务（需满足总开关 / 写操作 / 该房间开关），无需等下一次周期轮询；点赞本就只在直播中才推进，故开播即时触发能让自动点赞**立即可生效**。周期轮询降级为对「启动时已在直播」「开播信号捕获前」等场景的兜底，两处复用同一触发判断，不会重复触发正在执行的任务。
- **通过浏览器扩展获取 Cookie（替代“需完全退出浏览器”）**：新增「从插件获取」按钮，配合本仓库 `extension/` 目录的浏览器扩展（Edge/Chrome MV3）。安装扩展后，在插件弹窗里**勾选风险确认并点「获取并发送」**，程序即在**本机回环 127.0.0.1:64321** 短暂接收并写入 `cookie.txt`、应用到当前会话——**无需关闭浏览器**、不受新版 App-Bound 加密影响。安全控制：仅绑定本机回环、仅接受 `chrome-extension://`/`moz-extension://` 来源（普通网页无法伪造）、仅按钮触发且 90 秒超时自动关闭。原「获取Cookie」（读取浏览器数据库）**保留为备用**。

### 优化
- **快捷弹幕下拉改用「占位项」而非 placeholder（Tk / Qt 两版）**：Qt 的 `placeholderText` 由 Qt 以**灰色**绘制，看起来像「控件被禁用」——用户反馈「有选项时仍为灰色」；Tk 的 `ttk.Combobox` 没有 placeholder，无选中时就是**一片空白**——用户反馈「按钮上没文本」。现两版统一：把「快捷弹幕」作为下拉的**第一个真实项**（新增常量 `QUICK_DM_PLACEHOLDER`，按正常颜色绘制），两版显示与交互完全一致（选中它不做任何事）；无预设时下拉仍整体置灰（明确表示「这个房间还没配过」）。实测（Tk）：有预设 `values = ('快捷弹幕','打卡','好耶')` / `readonly`、无预设 `values = ('快捷弹幕',)` / `disabled`、两者显示文本均为「快捷弹幕」；选「打卡」填入后下拉复位为占位项，选占位项输入框不变。
- **补齐快捷弹幕的日志覆盖（Tk / Qt 两版）**：按项目「日志覆盖所有动作」的惯例补上四处——① **填入**（`window` info：「快捷弹幕：填入「打卡」（房间 X，仅填入不发送）」，Tk 主界面 / Tk 独立窗口 / Qt 三处各一条）；② **打开管理对话框**（`window` info：「快捷弹幕：打开管理对话框（房间 X，现有 N 条）」，两版；Qt 侧房间不在列表中时另给一条 warning，便于排查「点了没反应」）；③ **对话框内增删 / 上下移**（`window` debug，含文本与暂存条数）；④ **保存**（`task` info：「快捷弹幕已更新：房间 X，共 N 条」，两版，仅内容真的变了才记）。实测（`.venv` 跑 Qt 版）：`gui_qt.dm.window: 快捷弹幕：填入「打卡」（房间 1，仅填入不发送）` 与 `gui_qt.window: 快捷弹幕：打开管理对话框（房间 1，现有 2 条）` 均正常落盘。
- **排序说明收进「ⓘ」按钮（Tk / Qt 两版）**：排序栏右侧原本铺着一行随方案变化的提示文案，窗口一窄就被裁掉（用户反馈「提示太长、无法完整显示」）。现改为**一个「ⓘ」按钮固定在排序栏最右**（两版位置一致），**点击弹出当前方案的完整说明**（Qt `QMessageBox` / Tk `messagebox.showinfo`，标题带方案名），Qt 另有悬浮提示；说明文案重写为**多行条目**——逐条列出该方案的规则，并写明**能不能拖动、不能拖时怎么切回「自定义排序」**，两版共用同一份 `SORT_MODE_HELP`（替换原 `SORT_MODE_HINTS`）。拖动被拒时的反馈也随之调整：「ⓘ」临时变成「⚠」（Qt 的悬浮提示改为「当前排序方案不可拖动，请先切到「自定义排序」」）2.5 秒后恢复，并记一条 debug 日志——详细原因就在按钮里，不再弹窗打断。顺带删掉 Tk 排序栏里另一处残留的静态长提示（「（按住行拖动可调整顺序，Ctrl 可多选）」）。
- **排序栏改为「切换即生效」+「按直播状态」新语义（ROADMAP 85，Tk / Qt 两版）**：原来要先在下拉里选排序方式、再点「排序」按钮，另有「直播中置顶」勾选框（勾选时把直播中的房间一次性提到前面并**写进配置**，取消勾选也回不来）。现在：① **去掉「排序」按钮与置顶勾选框**，下拉**选中即生效**（立即重排 + 落盘），选择跨重启记忆；② 方案为「**自定义排序**（原「手动拖动」）/ 按房间号 / 按主播名 / **按直播状态**」——「自定义排序」就是你拖动出来的顺序，也是**唯一持久化**的顺序，其它方案只在显示层重排，所以**切走再切回「自定义排序」能原样复原**；③ 「按直播状态」把原置顶并入并给出确定规则：**正在直播的排最前、最近开播的在上**，其后是**已下播的按关播先后倒序**（最近关播的靠前），**轮播中按未开播处理**（不再单独成段），从没开播过的房间按自定义顺序落在最后；**收到开播/关播信号会自动重排**（弹幕推送与无连接房间的只读状态查询都算）；④ **只有「自定义排序」允许拖动**，其它方案下拖动会被忽略，并把工具栏提示临时改为「当前排序方案不可拖动，请先切到「自定义排序」」并记一条 debug 日志（避免「拖了没反应」的困惑）；⑤ 旧配置里的 `pin_live=true` 会在读取时自动迁移为「按直播状态」。实现上排序合成与时间记录都抽成两版共用的纯函数（`order_room_ids` / `update_live_activity`），新增 19 项离线测试。**顺带修掉一个实测坑**：先后标记最初用 `time.monotonic()`，而 Windows 上它的精度只有约 15 毫秒，同一轮事件里几次开播/关播会拿到**相同**时间戳、排序分不出先后（冒烟时踩到），改为「现有标记最大值 + 1」的自增标记，与时钟精度无关。
- **拖动排序改为「自管拖动」，并支持拖动中滚轮滚动列表（Qt 版，ROADMAP 75 后续）**：原实现借用 Qt 的拖放（`InternalMove`），有两个治不好的问题——① 拖动期间 Qt 会进入**平台拖放循环**（Windows 上是 OLE `DoDragDrop`），**滚轮事件根本不进 Qt 事件循环**，所以「拖动时滚轮滚动列表」收不到（实测连应用级事件过滤器也兜不住，属于「做了但没生效」）；② 落数据是「覆盖目标单元格 / 清空源单元格」语义（`dragDropOverwriteMode` 在 `QTableWidget` 上不起作用），只能事后按快照重建。现改为**完全自管**：关掉 Qt 拖放（`setDragEnabled(False)` / `setAcceptDrops(False)` / `setDragDropMode(NoDragDrop)`），按下 → 移动超过系统阈值（`QApplication.startDragDistance()`）进入拖动模式 → 移动、**滚轮**、边缘自动滚动（`DRAG_EDGE_MARGIN_PX` 内每 50ms 滚一行）全部自己处理，插入位置线与鼠标光标也自己维护（`begin_drag` / `update_drag` / `end_drag` / `insert_indicator_y` / `paintEvent` 画线），松手时按「拖动前的行序快照 + 插入位置」回报主界面重排（纯函数 `move_items` / `drop_insert_row` 不变）；**`Esc` 可取消拖动**。拖动中滚轮照常滚动列表，并**同步重算插入位置**（滚轮一格 = 120，按滚动条 `singleStep` 换算，不足一格的触摸板增量按一格算）。实测（合成鼠标 / 滚轮事件走**正常事件分发**，10 行小表格）：轻微移动 2px 不进入拖动；超过阈值（10px）进入拖动、被拖行正是鼠标按下的行；**拖动中滚轮 → 滚动条 `0 → 1`，插入位置 `5 → 6`（鼠标没动也跟着重算）**；松手提交 `(快照, [2], 6)`；`Esc` 取消 0 次提交；非拖动滚轮走默认滚动（不受影响）；在受保护的跳转列上按下拖动仍以按下的行为准（不拖走上一次选中的行）。
- **Qt 版界面紧凑化：更少空白、更多有效信息、默认窗口更高**（对比 Tk 版的直观差距）——① **控件内边距统一收紧**（新增 `COMPACT_STYLE`：按钮/下拉/输入框 padding 收到 1px、页签 4px×12px）：Qt 默认内边距比 ttk 大一截，实测同一缩放下按钮 23px（原 ~32px）、下拉 24px、复选框 16px，几行工具栏累积就吃掉大量垂直空间；② **房间列表高度随行数收缩**（新增 `_fit_table_height`，对应 Tk 的 `_fit_tree_height`）——Qt 表格默认填满布局，房间少时下方白出一大片；实测 2 个房间时列表区从 254px 收到 101px，**SC 文本区一屏可见行数 26 → 35**；③ 板块分配改为「房间列表按内容高度 + 剩余按 3.5:4.5 给 SC / 弹幕」（弹幕区隐藏时剩余全部给 SC）；④ 布局边距/间距由 Qt 默认的 9/6 收到 4/4，**「显示弹幕区」合并到底部操作条**、**SC 头部与「累计 SC」合并为同一行**、排序栏提示改为「（拖动行排序，Ctrl 多选）」并靠右（不再被挤断）；⑤ 表格行高内边距 8 → 6，**直播标题与备注列都改为弹性**（此前标题独吞多余宽度、备注被挤到只剩几个字）；⑥ 默认窗口 **1000×680 → 1080×820**（并按屏幕可用区域的 88% / 92% 取上限，避免小屏超窗），且新增**窗口尺寸记忆**：退出时写入 `gui_rooms.json` 的 `ui.window_size`，下次启动恢复（Tk 版不使用该键，只原样写回）。SC 头部因改为单行显示，过长时可通过悬浮提示查看完整内容。
- **日志覆盖扩展到所有动作，并支持分区块启用**：新增 `logging.categories`（`room` / `window` / `data` / `task` / `live` / `app`，**默认全开**）与区块过滤机制——**判定顺序为先看主开关 `enabled`（只决定是否落盘），再看区块开关**（关闭的区块在控制台、GUI「调试」页与文件里都不输出；未标记的第三方库日志不受影响）。覆盖面从「连接/任务」扩展到全部动作：**切换直播间**（房间号、主播、直播状态）、**房间管理**（添加/解析失败/删除/启停/排序/拖动排序/备注/点击跳转/开播提醒开关）、**界面布局**（窗口尺寸与分隔条——按 `DebouncedValueLogger` **节流合并成「首次 → 最后」一条**，拖动时不刷屏；页签切换、弹幕区与表情面板显隐、调试日志级别切换）、**获取数据**（`api.py` 三个统一请求入口记录「路径 + 房间号/uid/分页等关键参数 + 耗时 + 返回 code」，**不记录 cookie / csrf / WBI 签名**；历史 SC 分页读取、落盘成功、弹幕**按批汇总**、表情包与弹幕选项刷新、粉丝牌与任务刷新）、**触发任务**（发送弹幕含内容摘要与颜色模式、发送表情、粉丝牌手动/自动任务与**打断**、任务开始/结束摘要、Cookie 获取与来源、开播提醒三通道）以及**应用生命周期**（启动参数与房间数、退出、后台就绪/失败、房间锁获取/占用/释放）。实现上抽出 `log_categories.py`（区块常量 + `CategorySwitches` / `CategoryFilter` / `get_logger`）与 `log_setup.py`（`configure` / `attach` / 轮转文件 / 节流器），**Tk 版、Qt 版与命令行共用**；Tk / Qt 的全部既有日志调用、以及 `api` / `storage` / `client` / `gui_config` / `room_lock` / `browser_cookie` / `browser_rooms` / `cookie_server` / `overlay` / `medal_runner` 均按区块归类（共 79 处既有调用完成归类）。Qt 版另补 `resizeEvent` 与 `splitterMoved`（此前缩放窗口、拖动分隔条完全没有记录）。
- **新增「弹幕表情图」开关（Qt 版）**：内嵌表情图更直观，但一行会明显变高（表情图高 40~64px，纯文字行只有约 23px），弹幕密集时占掉不少高度。现在窗口底部（与「显示弹幕区」并列）新增**弹幕表情图**开关，状态记在 `data/gui_rooms.json` 的 `ui.dm_emoticon_image`，**默认开启**，且**两个方向都立刻作用于已有弹幕**：关闭时把已显示的表情图还原成「[触发词]」文字（不必等新弹幕，行高马上降下来；原本吸底就继续吸底），并停止为「等图」的行补图；**重新开启时再把弹幕流里已有的触发词换回图片**（图仍在缓存就直接换，否则触发下载、到位后自动补上），无需切房重载。为控制开销只处理**最近约 400 条表情弹幕**（更早的已滚出视野，不再追溯）。关闭期间**悬浮提示恢复为「有原图就看原图」**的旧行为（弹幕里已经没有图了）。Tk 版不做内嵌，无此开关。
- **Qt 版弹幕流内直接显示表情图（悬浮提示相应改为只显示文字）**：Qt 的富文本原生支持内嵌图片，性能足够，不必像 Tk 版（`Text` + `PhotoImage`）那样只能靠悬浮看原图。现在表情弹幕**直接在弹幕流里显示图片**：图已缓存就直接插入，未缓存则先显示触发词文本并**记下该行**（`QTextBlock`，有 400 条上限保护），图片下载完成后**原地换图**——只改这些行内的那一段，滚动位置、其它行与未读计数都不受影响；已被行数上限裁剪掉的行会自动跳过。图片格式上同样挂着正文标记 / dmid / 表情 unique，所以**点击图片照样能复制该条弹幕、右键照样能回复、悬浮照样出提示**。悬浮提示相应简化为**只显示配置字段文字**（触发词 / unique / id，见 `config.json` 的 `emoticon_tooltip`），不再重复弹一张大图（图已在弹幕里）；一个字段都没开时不显示提示，与表情面板按钮的 tooltip 行为一致。顺带修掉一处偏移：`QTextCursor.setPosition(i)` 后直接取 `charFormat()` 拿到的是**前一个**字符的格式，会导致换图时行首残留一个字符（如 `[`），现统一改为「选中该字符」再取格式。
- **Qt 版默认窗口宽度改为最小许可宽度**：三区块是纵向排布，窗口更宽只是让每行文字更长、并不增加信息量，反而让 SC / 弹幕区两侧多留空白；默认宽度因此直接取 `MIN_WINDOW_SIZE[0]`（880），高度仍为 900。
- **运行日志落盘（受 `config.json` 控制，用于事后监测运行状态）**：新增 `logging` 配置段与统一的日志初始化模块 `log_setup.py`，**Tk 版、Qt 版与命令行共用同一套实现**（级别、轮转文件、格式化器只维护一处）：`enabled` 是否写文件（默认 **false**，改为 `true` 即写到 `logs/app.log`，相对路径按项目根目录解析）、`level` 启动级别（DEBUG/INFO/WARNING/ERROR/CRITICAL，默认 INFO）、`file` 路径、`max_bytes` × `backup_count` 轮转上限（默认 2 MB × 3 个备份，可写绝对路径）。GUI 的「调试日志（DEBUG）」勾选框仍在运行期临时提升级别，**取消勾选时回到配置里的级别**（不再写死 INFO），启动时勾选状态也跟随配置；命令行 `--log-file` 显式指定时覆盖配置，`--verbose` 临时提升为 DEBUG。文件日志**带完整日期**（跨天回溯）并含 `Room[房间号]` 来源前缀；启动时会先把「日志级别 / 落点」写进日志便于确认生效。设计上保持 fail-safe：级别名拼错、路径非字符串、大小非整数等**逐项回退默认**并告警，目录不可写只提示、不影响程序运行，重复初始化不会重复挂 handler（`logs/` 与 `*.log` 已在 `.gitignore` 中）。
- **自动发弹幕默认只在未开播时执行，并可另行允许开播时执行**：发弹幕会出现在主播弹幕区，为避免直播中自动刷屏，**自动发弹幕默认仅在直播间未开播时生效**；新增按房间的「**开播时也自动发弹幕**」开关，勾选后开播时也执行。**手动**「发弹幕」按钮不受限制。
- **自动开关按任务分离（点赞 / 发弹幕各自独立）**：原「自动完成选中房间（两项都做）」拆成「**自动发弹幕（选中房间）**」与「**自动点赞（选中房间）**」两个独立勾选，各自只自动执行对应任务（只开一项就只做那一项）；`config.json` 的 `medal_tasks.auto` 仍为两项的**总开关**。旧配置里的 `auto_medal_tasks` 会自动迁移为两项都按原值。任务列表「自动」列显示该房间已开启项：`弹` / `赞` / `赞+弹` / `关`。
- **粉丝牌页任务列表顺序跟随直播间列表（实时）**：「监听房间 · 任务」列表行顺序与直播间页签**完全一致**，并在直播间页排序（按房间号/主播名/直播状态、直播中置顶）、拖动调整顺序、添加或删除直播间后**即时同步**（该页不单独排序）。
- **「粉丝牌」页签可用性**：
  - 两张表格（我持有的粉丝牌、监听房间任务）中的**主播**与**房间号**支持**单击跳转**，并加入与直播间页签一致的**点击保护**：跳转列**按下时不改变选中行**、**松开且未拖动到其它单元格**才真正打开链接（避免拖动列宽/误触时误开浏览器）；主播 uid 未知时后台补齐后再点即可跳转；
  - 两个任务按钮顺序调整为 **发弹幕 → 点赞**，与上方任务列表的列顺序一致，避免与列表对不上；
  - 表格列改为**可稳定拖动**（固定 `stretch=False` + 最小列宽），并在超出窗口时提供**横向滚动条**，不再出现列被拖乱/内容被裁切看不到的问题。
- **首页底部操作条与弹幕开关合并为一行，消除弹幕区下方的多余空白（ROADMAP 91）**：Tk 版此前「弹幕开关」与「全局按钮」各自 `pack(side="bottom")` 成一个 Frame，两行叠在弹幕区下方留出一片空白。现合并为**同一行**并与 Qt 版底部布局对齐——依次为「显示弹幕区」勾选框、其说明文字、「刷新历史」「获取Cookie」「从插件获取」三个按钮；勾选框文案由「弹幕」改为与 Qt 版一致的「显示弹幕区」。实测（空房间目录启动、读取控件坐标）：页签内底部只剩**一个** Frame（y=627、h=27），5 个控件同在 y=0~3 的一行（x 依次 0 / 89 / 331 / 424 / 517，总宽 604 < 页签宽 984），底部到页签底仅 6px 边距，弹幕区（TPanedwindow）下方不再留空行。Qt 版布局本就如此，本次未改动。**而真正那片空白在发送栏下方**：那里有两行提示 `Label`（`dm_send_hint_var` 发送结果 / `dm_copy_hint_var` 已复制），**无内容时仍各占一行高度**（共 42px），空着看过去就是一片空白（历史遗留）。现改为**空提示不占位**：Tk 版新增 `_sync_dm_hint_rows()` 随文本变化 `pack` / `pack_forget`（每次按「发送提示 → 复制提示」固定顺序整体重排，避免复制提示先出现时顺序颠倒），Qt 版新增 `_set_hint()` / `_set_copy_hint()`（空文本 `setVisible(False)`，构造时初始隐藏）。实测（空房间启动、读取控件坐标）：两条提示都为空时发送区高 **69 → 27**、弹幕区需求高 **206 → 164**（正好少两行）；启动期的真实提示（如「后台初始化中，请稍候…」）照常占位；两行同时出现时「发送提示」仍在「复制提示」之上（`send_y=27 < copy_y=48`）。
- **醒目留言标题栏显示实际粉丝牌名称**：原显示「粉丝牌 Lv13」，现改为「小路泥 Lv13」这样的**真实勋章名**（在「同接」「舰长」之后）；未持有该主播粉丝牌时不显示。
- **启动即加载粉丝牌**：程序就绪后自动拉取一次粉丝牌与各房间任务，**无需先点开「粉丝牌」页签**，SC 标题栏即可显示等级。
- **执行效率可观测**：每次任务执行后在页签提示与调试日志中给出**请求次数 / 点赞次数、发送条数（表情 / 文本）、单任务用时与总耗时**（如「点赞：…（1 次请求 / 10 次点赞，用时 3.2s）」「总耗时 12.4s」），便于评估节奏。
- **亲密度拆成两列并显示当前经验**：原「今日/升级需」把两类数值挤在一列，现拆为——**「今日亲密度」= 今日已获取**（`today_feed`，如 `+23`）、**「当前/升级需」= 当前亲密度 / 升到下一级所需经验**（`intimacy/next_intimacy`，如 `26/99`）。
- **开播信号打断正在执行的「自动发弹幕」（ROADMAP 64）**：自动发弹幕按轮次连续发送（默认每 6~8 秒一条），此前若**发送途中主播开播**，本轮会一直发到任务完成或失败为止——直播弹幕区里继续刷屏，而该项的定位本就是「未开播时自动补任务」。现为执行引擎加入**房间级打断标记**：收到开播信号（与开播提醒同一「非直播中 → 直播中」跳变判定）即打断该房间正在执行的自动发弹幕，**等待间隔被提前唤醒**（不必等这条间隔走完），已发出的弹幕不撤回；结果单独标记为「已打断」并保留已发送条数（如「发弹幕：直播间已开播，已打断自动发弹幕任务（1 条：表情 0 / 文本 1）」），**不计为失败、不做重试**。打断范围严格限定为「**自动提交且包含发弹幕**」：**手动**「发弹幕」按钮触发的任务（`auto=False`）与点赞任务完全不受影响（点赞本就只在直播中推进）；勾选「允许开播时自动发弹幕」的房间也不打断（用户已明确许可开播时也发）。Tk / Qt 两版同步接入，`medal_start` 事件顺带带上 `auto` 标记便于排查。

### 修复
- **快捷弹幕预设被 20 字截断 / 计数写成「x/20」看着像有上限（ROADMAP 91）**：发送侧其实**两版都早已取消客户端字数限制**（`danmaku_send_guard` 只拦「空内容」与「发送过快」，注释写明长度与重复都交由服务端判定；Qt 输入框也没有 `setMaxLength`），但两处历史遗留仍在制造「20 字上限」的印象：① **预设被截断**——`gui_config.normalize_quick_danmaku` 按 `QUICK_DANMAKU_LEN = 20` 截断，而且是**读配置时就砍**；更糟的是快捷弹幕管理对话框「关闭即保存」会把工作副本写回配置，于是长句被读一次就永久变短（用户反馈「快捷弹幕预设也不能超过 20 字」）。现**取消单条长度限制**（保留去首尾空白、保序去重、每个房间最多 12 条），`QUICK_DANMAKU_LEN` 常量与 `max_len` 参数一并删除，两版管理对话框添加时也不再截断。② **计数提示**——Qt 面板的标签始终显示 `x/20`、Tk 房间独立窗口超长时也显示 `x/20`，看起来像「超过 20 字发不出去」；现三处（Qt 面板 / Tk 独立窗口 / Tk 主界面）统一为**只显示已输入字数、超长标红**（标红提醒保留——服务端仍可能拒绝）。实测：34 字预设在配置里读取后仍是 34 字（旧行为会变成 20 字）、存盘再读完全一致；清洗仍限制条数（12 条）但不限长度（500 字原样保留）。新增 4 项测试（长文本保留、长预设存取往返、三处计数都不再含「/上限」写法、超长标红提醒仍在）。
- **Qt 版「快捷弹幕」管理按钮点击无反应（ROADMAP 90 收尾修复）**：根因是 `qt_app.py` 的 `PySide6.QtWidgets` 导入里**漏了 `QDialog` 与 `QListWidget`**——补这两个 import 的那次编辑被**同一批次下发的另一个编辑覆盖**了（同文件并行编辑会丢失更新，本轮的字段与 import 丢过多次）。于是 `open_quick_danmaku_manager` 一进入就 `NameError`，而异常发生在 Qt 槽函数内、`pythonw` 启动时没有控制台 → 用户看到的现象就只是「按下没反应」。现补齐 import（对话框本身的增删 / 落盘逻辑无误）。**顺带纠正一个长期的验证盲区**：本仓库依赖（含 PySide6）装在项目内 `.venv`，而此前一直用系统 Python 跑 `unittest`，导致 `test_gui_support` / `test_qt_wiring` / `test_qt_preview` 等 **Qt 相关测试被静默跳过**（只跑 313 项、2 项报「No module named PySide6」）；改用 `.venv\Scripts\python.exe` 后共 **524 项全部通过**——这也是上一轮 Qt 递归 bug 未被及时发现的原因之一。实测（`.venv` + 自建带预设的房间，9 项）：下拉 `['快捷弹幕','打卡','好耶']` 且可用、无预设房间 `['快捷弹幕']` 且置灰；点「管理」成功弹出对话框（修复前为 0 个）、添加「新快捷语」、删除首条后关闭，内存与落盘均为 `['好耶','新快捷语']`。
- **Qt 版弹幕提示行的无限递归（对上一提交的更正）**：上一提交把提示行改为「空内容不占位」时，用批量替换把 `self.dm_hint.setText(` 改成了 `self._set_hint(`，**连带把当时新建的 `_set_hint` 方法体内那一行也替换掉了**，方法体变成自我调用（`self._set_hint(text)`），于是 Qt 版**任何发送或门控提示都会 `RecursionError`**（该方法被 10 处调用）。该回归因本机未装 PySide6、Qt 测试无法执行而未被发现。现把方法体改回 `self.dm_hint.setText(text)`；`_set_copy_hint` 未受影响（替换模式与之不匹配）。两条教训记此备查：**① 不用批量替换去改「正被引用的新方法」——替换会命中方法体自身**；**② 同一文件的多个编辑不放在同一批次并行下发**（本次实际发生过编辑丢失：字段与 import 被同批次写入覆盖，需逐一 grep 核对才能发现）。
- **部分房间的点赞任务被「已达储蓄亲密度上限」整房拦下（ROADMAP 89）**：粉丝牌执行引擎 `_complete_locked` 拉到任务信息后，只要接口的 `reach_free_intimacy_limit`（该粉丝灯牌刚点亮）为真，就**直接返回 `done`** 并给出「已达储蓄亲密度上限：投喂一个粉丝灯牌即可领取，暂不执行」——该房间的点赞与发弹幕任务**一条都不执行**。但该字段表示**该粉丝灯牌刚点亮**（本次实测：接口 data 里确实返回 `free_intimacy` / `reach_free_intimacy_limit`，20 个房间均有值；官方规则页只讲「每日上限」，并无「储蓄」一说；官方规则第 7 条明文「熄灭状态下……点赞行为和发送弹幕行为**仅点亮勋章不可获得亲密度**」，用户已确认 3044248 / 5050 / 1016 三个勋章都是**靠点赞 30 次重新点亮**的，正常点亮状态下照给亲密度、实测 1700301235 今日 +20；实测 `light_source=1` 的 4 个勋章 `free_intimacy` 全 >0、`light_source=0` 的一律 0 / false；`free_intimacy` 数值本身的语义仍未确证，不影响本节结论），**与任务本身能否完成无关**：点赞 / 发弹幕仍会照常计入任务进度，网页端与手机端手动点击同样能完成任务（用户实测，**部分房间**命中该字段即表现为「显示上限、却又点得动」）；但**它绝不表示任务不该做**：长时间不做任务粉丝灯牌会熄灭，做任务正是为了点亮并维持灯牌。现改为**任务照常执行**、只把缘由说明出来：命中时记一条 `task` info 日志（「粉丝灯牌刚点亮：免费任务暂不增加亲密度，任务照常执行以点亮并维持灯牌」），并在结果说明末尾附「（粉丝灯牌刚点亮，点赞 / 发弹幕暂不涨亲密度，任务仅用于点亮并维持灯牌）」；后续流程不变——另补上**灯牌熄灭**（接口把任务下发为「仅点亮」、上限 0）的情形：原先 `is_task_applicable` 判其「当前不适用」直接跳过，导致灯牌熄了就再也点不亮；现新增纯函数 `is_light_up_task`（未完成且上限 ≤ 0）并纳入 `pending_write_tasks`，按官方点亮任务次数执行（点赞 30 次 / 发弹幕 10 条，常量 `LIGHT_UP_LIKE_CLICKS` / `LIGHT_UP_DANMAKU_COUNT`）——点亮阶段接口不下发进度，故不按「进度推进」判定、发满即收尾。任务已完成就显示「已完成」，服务端确实不推进时仍由原有的「点赞进度未推进（x/y）」上报（连续无进展达重试上限即停，不空发）。两版共用同一执行引擎，**Tk / Qt 同步生效**；新增 / 改写离线回归测试 5 项（该标记为真时点赞请求 `click_time=30` 与发弹幕 2 条均照常执行且结果附缘由说明、任务已完成时同样附说明、未命中时说明保持原样；另 2 项覆盖**灯牌熄灭**：上限 0 时按官方点亮次数发满点赞 30 次与弹幕 10 条、`is_light_up_task` 与 `pending_write_tasks` 的判定）。
- **重启后「记忆了排序方案，列表却仍是自定义顺序」（ROADMAP 88）**：Tk 版启动填充固定按**配置文件里的顺序**（＝自定义顺序）执行，`_populate_rows` 里还留着「不再启动时自动排序」的注释——那是「排序靠手动按钮触发」时代的做法；如今方案已是「下拉选中即生效 + 跨重启记忆」，启动不应用方案就等于方案没生效（Qt 版启动虽然算了显示顺序，但状态陆续到达时若「时间基准没变」也不会重排，开局同样像是自定义顺序）。修法：① Tk 的 `_populate_rows` 改为按 `_sorted_room_ids()`（当前方案的显示顺序）填充；② **状态跳变**（不只是时间基准变化）一律触发重排——启动后各房间状态陆续到达时，本来就在直播的房间也会被排到最前；③ 重排改为**合并执行**：事件里只置 `_sort_dirty`（记 debug），由本轮 100ms 轮询末尾统一 `_apply_sort()` 一次，避免启动时 N 个房间逐个重排（Qt 版还要重建整张表）；真正重排时记 info「状态跳变已合并，按直播状态重排列表」。
- **补齐前几轮改动缺失的日志（复核最近的提交：房间独立窗口 / 排序方案 / 「已播」时长 / 拖动排序）**：这几轮只改了行为、有些路径没留痕，现补上——① **「已播」与排序时间的来源全程可追**：`api` 解析开播时刻后记 debug（`get_info` 的 `live_time` 原文 → 归一化结果），`get_full_room_info` 分别记「改用 H5 时间戳」与「H5 未给出、沿用 `get_info` 结果」；`client` 的连接日志、状态刷新日志与 `_emit_status` 广播日志都带上开播时刻；宿主在**没有接口值、回退本地观测**时记一条 debug（明确告诉用户这一条是估算的，接口返回后会校正）。② **房间独立窗口（ROADMAP 84）**：几何记忆的**写入**与**恢复**各记一条 debug（此前只有「打开 / 前置 / 关闭窗口」有日志，遇到「窗口位置没记住」无从查起）；Tk 版独立窗口的历史 SC 分页补一条「加载完成」debug（失败路径原本已有 exception）。③ **弹幕接收门控**：`client.set_danmaku_enabled` 改为返回**是否变化**，两版 `_apply_dm_gate` 只在门控**真的变了**时记一条 debug（列出变化的房间与当前接收集合）——切房 / 开关弹幕区 / 开关独立窗口都会走到那里，无条件记会刷屏；这条日志正是排查「独立窗口里没有弹幕」的关键。新增 6 项测试锁住上述接线（数据层开播时刻日志、窗口几何日志、门控日志与返回值）。
- **「按直播状态」排序重启后被打乱（ROADMAP 87）**：此前排序只依赖**运行期内存里的先后标记**（自增整数），程序一退出就全丢——重启后所有房间都没有开播/关播记录，顺序回落到「自定义顺序」，看起来就是排序被打乱。根因有两个：① 时间基准**没有持久化**；② 记的是「本程序观察到状态跳变的时刻」而不是**真实开播时间**，所以即便存下来也未必准（启动时就已在直播的房间，根本无从得知它已经播了多久）。现改为：**时间基准一律是真实墙钟时间，并持久化到 `data/gui_rooms.json` 的 ui 段**（`live_started_at` / `live_offline_at`），启动时读回（丢弃过旧 / 未来 / 非法的记录），因此**重启后顺序不变**。「开播时刻」直接复用上一版为「已播」时长引入的 `live_started_at`——它取的是**接口给的真实开播时间**（H5 `live_start_time` 时间戳优先，`get_info` 的北京时间字符串回退），所以**启动时就已在直播的房间，等于用已播时长反推出开播时间**，排序立刻正确；接口取不到时才回退为「本程序第一次看到它直播中」的观测时刻，接口一旦返回即校正。实现：新增两版共用纯函数 `update_offline_at`（关播时刻；带 `was_live` 参数**只认真实的状态跳变**，避免把周期复核的重复上报当成新关播、也不给「从未开播」的房间留记录；同一轮几个房间同时下播时钟可能撞值，此时用「现有最大值 + 1 毫秒」保证仍能排序）与 `prune_live_marks`（启动清理：超过 24 小时、未来时间、非法键值一律丢弃），`gui_config.parse_room_time_map` 负责 ui 段的宽松解析；排序时 `live_since` 直接传 `live_started_at`，旧的运行期自增标记 `_live_since` **整体弃用**；两版在开播/关播（含无弹幕连接的只读查询路径）导致时间基准变化时**立即落盘**，删房时一并清理。实测（假 self 调用两版**真实的** `_save_config` + 真实 `load_ui_prefs` / `prune_live_marks` / `order_room_ids` 走一遍「运行 → 落盘 → 重启」）：33 先开播（已播 2 小时）、11 后开播（10 分钟）、22 刚下播 → 重启前后顺序都是 `[11, 33, 22, 44]`；26 小时前的记录、未来时间、非法键值在读取时被丢弃。新增 15 项测试（关播时刻规则、同轮撞值仍可排序、持久化往返、重启顺序一致、脏数据过滤、两版接线）。**另补齐相关日志**（顺序没排成预期时，靠日志就能还原「时间基准是什么时候、因何变成这样的」）：启动恢复记一条（恢复条数 + 被丢弃的过期/非法记录条数，无记录时降为 debug）；每次时间基准变化记一条（房间号、原状态、更新后的开播/关播时刻、已落盘，只读查询路径单独标注来源）；新加房间「本来就在直播」记 debug；删房清理记 debug。时间串统一由 `format_live_mark` 输出（本地时间，非法值给「—」，不抛异常）。实测输出：`gui.data | INFO | 已恢复「按直播状态」排序的时间基准：开播 2 个、关播 1 个，丢弃过期/非法记录 2 条`。
- **补齐本轮改动缺失的日志**（沿用 ``logging.categories`` 的分区块机制）：右键菜单的三个动作与取消（``room`` info / debug）、「拖动源改用鼠标按下的行」（``room`` debug）、拖动到列表边缘的自动滚动**启停**（``room`` debug——每 50ms 的 tick 本身不记，避免刷屏）、点击「提醒」列切换开播提醒（``room`` info）。其余改动此前已带日志：拖动开始 / ESC 取消（debug）、拖动中滚轮滚动（debug）、拖动排序与手动排序与「直播中置顶」的结果（info，另加 ``storage`` 的落盘 info）、点击跳转（info）、预览的加入 / 停止单路 / 停止全部 / 主路切换 / 已达上限（``live`` info / warning）、置顶开关（``window`` info）、音频输出跟随切换（``window`` info + 单路失败 debug）。
- **预览声音不跟随系统音频输出设备切换**（**两次踩的坑都记在这里**）：``QAudioOutput`` 在创建时就选定了输出设备，之后**不会**跟随系统默认设备变化——把系统输出切到耳机 / 蓝牙后，预览仍从旧设备出声（甚至直接没声）。最终实现分两处：① **检测**：``audioOutputsChanged`` 是**实例信号**，第一版写成对类属性 ``QMediaDevices.audioOutputsChanged.connect(...)``，抛出的 ``AttributeError`` 又被 ``except`` 静默吞掉——信号**从没接上**，只剩 10 秒巡检；现在改为 ``QMediaDevices(self)`` 实例连接并保住引用，失败时按 ``warning`` 记账。② **切换**：**先停播放器 → ``setDevice()`` → 先清空 source 再设回同一地址 → ``play()``**（与「换清晰度」同一套做法）。三种做法逐一排除：只调 ``setDevice()`` 而播放不停常常不生效（声音还从旧设备出）；在播放中换一个新的 ``QAudioOutput`` 实例交给 ``setAudioOutput()`` 会让**播放器卡住**（画面定格、之后彻底没声）；``stop()`` 后只 ``play()`` **常常起不来**（要等 5 秒健康检查才自愈）。代理地址是本机的、长期有效，重新拉流一两秒即回到画面；万一还是没起来，该路 5 秒健康检查仍会兜底。另外保留 10 秒巡检（``AUDIO_DEVICE_CHECK_MS``）与一条 debug 日志（打印系统默认输出与预览各路实际设备），排查「切了系统输出、预览没跟随」时可开调试日志分辨是「Qt 没感知」还是「切换没生效」；打开预览时先对齐一次默认设备，停止预览时巡检随其它定时器一起停。实测（本机 6 个输出设备）：初始各路设备＝系统默认；切到另一设备成功、重复切换幂等；模拟默认设备变化后各路自动跟随回系统默认。
- **预览浮窗置顶的两处问题：「取消置顶就消失」与「置顶后右键菜单不显示（主界面像失灵）」**：① 置顶开关原先直接 ``setWindowFlag(Qt.WindowStaysOnTopHint, checked)`` 再 ``if self.isVisible(): self.show()``——而 Qt 改窗口标志会**先把窗口隐藏**（文档：改完要再 show 才会显示），那个判断在此时恒为 ``False``，窗口再也不会显示（「取消置顶就消失」）。② 勾选置顶后，重建的窗口会被系统摆到最前并**激活自己**（焦点从主界面被拿走），而置顶窗口还会把主界面弹出的**右键菜单压在下面**——菜单（``Qt.Popup``）仍会抢占鼠标并保持打开，于是「右键后看不见菜单、却在原位置点到了菜单项」，紧接着主界面像失灵（用户反馈）。修复：① 在 ``setWindowFlag`` **之前**记下可见性与 ``geometry()``，切换后按原可见性 ``show()`` 并**恢复位置尺寸**；② 浮窗设 ``Qt.WA_ShowWithoutActivating``，显示（含重建后 show）时不抢焦点；③ **弹出菜单自身也置顶**——房间列表右键菜单、弹幕右键菜单、预览格子菜单三处统一加 ``setWindowFlag(Qt.WindowStaysOnTopHint, True)``，``exec`` 激活后即压在浮窗之上。**试过但放弃**：用 Win32 ``SetWindowPos(HWND_TOPMOST/HWND_NOTOPMOST)`` 绕开重建——实测 ``WS_EX_TOPMOST`` 位并未真正清掉，且它与 Qt 内部标志不一致（之后任何 ``setWindowFlag`` 都会按旧标志覆盖回去）。实测（假宿主 + 模拟主窗口）：初始可见 `True`、位置 `(600,230)`、活动窗口＝主窗口 → **取消置顶后三者全部不变**（旧写法此处窗口已不可见）→ 重新置顶仍不变；菜单标志实测：设置后 `windowFlags() & WindowStaysOnTopHint` 为 `True`、菜单是顶层 popup（对照未设置时为 `False`）。
- **自管拖动带来的「跳转保护失效」回归（按住跳转列移动会改选中）**：拖动排序改成自管后关掉了 Qt 的 `QDrag`，于是 Qt 默认的「按住移动会按当前索引更新选中」重新生效——按住跳转列（房间号 / 主播 / 提醒）稍一移动，选中行就被改掉，表现为「跳转主页 / 直播间的保护失效」（用户反馈）。修法三处：① `selectionCommand` 的拦截从「按下 / 松开」扩到**按下 / 松开 / 移动**三种事件（Qt 的 `mouseMoveEvent` 内部同样会问它）；② 判定改用「**按下时的列** 或当前列」是否为跳转列（按下后移到别的列时 `index` 已不是跳转列，只看当前列会漏，保护只认起点）；③ 另在 `mouseMoveEvent` 里对「按下点在跳转列」的移动**不交给基类**——实测 Qt 那条改选中的路径**不经过 `selectionCommand`**，只在里面拦不够。实测（合成事件逐步定位）：初始选中 `[4]` → 按下跳转列 `[4]` → 同列移动 6px `[4]` → 按住移到别的列超过拖动阈值才进入拖动排序（此时选中切到被拖的行是**设计行为**）；纯点击跳转列：选中不变且照常打开链接；轻微抖动 3px 仍算点击；点击非跳转列（状态列）选中照常切换（未过度保护）；点击跳转列后再点其它列不受影响（`_press_cell` 在本次交互结束后清理）。
- **Qt 版右键房间行会「切换」当前直播间（连带把预览切走）**：右键房间行只是弹上下文菜单（预览该房间 / 停止预览该房间 / 设为主路），但 Qt 默认会把**选中行**改到右键所在行，于是连带触发一次「切换直播间」——SC / 弹幕面板被切到那一行，开着「跟随选中房间」时连**预览**也被切走（用户反馈「右键直播间时会切换」）。现 ``ProtectedLinkTable.selectionCommand`` 对**右键的按下与松开都返回 ``NoUpdate``**（与跳转列保护同一机制；按下与松开都要拦——Qt 在「按下未改变选中」时会在松开时补一次），右键因此完全不改变选中；菜单项本来就按 ``indexAt`` 定位到右键所在行，与选中无关。顺带把「预览该房间」里的 ``_select_room`` 也去掉：预览只把该房间**加入**预览，不再顺手切换选中（当初是为了让「跟随」跳过；如今「跟随」的语义已改为「选中已在预览中 → 设为有声音的那一路；不在预览中 → 什么都不做」，不再需要）。实测（合成鼠标事件走正常事件分发）：右键第 5 行前后选中恒为 `[1]`、**选中变化次数 0**（即不会触发切换）；右键已选中行也不变；左键点非跳转列仍正常切换（`[1] → [3]`，未误伤左键）；左键点跳转列的保护不受影响；右键位置仍能正确定位到该行供菜单使用。
- **拖动「部分情况下拖走的是上一次选中的行」**：Qt 在按下时通常会把该行设为选中，但**受保护的跳转列**（房间号 / 主播 / 提醒）按下时我们故意不改选中（就是「点主播名打开主页不切走 SC 面板」那条保护），于是 `startDrag()` 只读 `selectedIndexes()` 时，拖走的会是**上一次选中的那一行**——落点与拖影也跟着对不上。现以**鼠标按下的行**为准：新增纯函数 `drag_source_rows`——按下的行有效且不在选中集合里就搬它，否则沿用选中集合（多选整体拖不受影响）；同时把选中切到该行，让 Qt 的**拖影**与我们的快照一致。实测（真实表格：先选中第 1 行 = 111，再按第 3 行 = 333 拖动）：旧逻辑搬 `[0]`（= 111，即该 BUG），新逻辑搬 `[2]`（= 333）；多选 `[1,2]` 时按其中任一行拖动仍整体搬 `[1,2]`；按在列表下方空白（取不到行）退回选中集合。另加 4 项离线测试（按下行优先、选中集合保持、无按下/越界回退、去重排序）。
- **Qt 版手动 / 拖动排序不记忆，重启窗口就重置**：配置文件里 `rooms` 的**顺序本身就是**房间列表顺序（启动时 `_room_order = list(entries.keys())`），而 Qt 版 `_save_config()` 直接写 `entries.values()`，`entries` 的键顺序却**从未**按显示顺序更新过——于是拖动 / 手动排序 / 「直播中置顶」的结果只活在内存里，重启即被配置里的旧顺序覆盖（Tk 版在拖动结束时按 `tree.get_children()` 重排了 `entries`，Qt 版漏了这一步）。现 `_save_config()` 统一按 `_room_order` 重排 `entries` 再写盘（新增纯函数 `ordered_room_ids`：不在顺序里的房间**补到末尾**、已删除的忽略、重复的去掉，两头都不丢房间），因此任何改变顺序的路径（拖动、手动排序、置顶、增删房间）都会落进配置。实测（用假 self 调真实的 `_save_config`，再 `load_room_entries` 读回）：拖动「首行到末尾」后保存并重载 = `[222,333,444,111]`、手动按房间号降序后 = `[444,333,222,111]`，都与显示顺序一致；**对照旧实现**（不重排直接写）读回仍是 `[111,222,333,444]`——排序丢失，正是该 BUG 现象；顺序里只给一个房间时保存为 `[333,111,222,444]`（其余补末尾、不丢房间）。
- **拖动排序：把 Qt 的「覆盖 / 清空」语义换成自管插入（ROADMAP 75 的真正根因）**：拖一行放到另一行中间时，不是插入行，而是被拖行的数据**盖在目标行对应的单元格上**、连带串掉后面几列的内容。根因是 `QTableWidget` 继承 `QTableView`，后者 `dragDropOverwriteMode` **默认为 true**（实测 `QTableWidget().dragDropOverwriteMode()` → `True`）：放下鼠标**不插入新行**，而是把被拖行的数据从落点**那一列开始** `setData` 覆盖到目标行；实测 3 列、把首行拖到第 3 行中部 → 目标行变成 `['111','主播111','备注333']`（前两列被糊成被拖行的内容、备注列仍是自己的，**行数根本没变**）。**只关掉 `dragDropOverwriteMode` 不够**（用户复测「仍有该问题」）：`QTableWidget` 的内部移动仍走它自己那套「插入/删除 + 清空源单元格」语义，落点与最终结果都不受我们控制。故改为**自管拖放**——`ProtectedLinkTable` 只借用 Qt 的拖动交互与落点提示：`startDrag()` 里记录**拖动前的行序快照**，`dropEvent()` 里自己算插入行号（新增纯函数 `drop_insert_row`，与提示线同一套阈值：该行上边缘与行中间 → 插到该行之前、下边缘 → 之后、列表下方空白 → 末尾），等 `drag->exec()` 阻塞返回（此时这次拖放已彻底结束）把「快照 + 源行 + 落点」回报主界面，由 `_on_rows_reordered` 用纯函数 `move_items` 算出新顺序并**重建行**——Qt 把表格糊成什么样都会被覆盖掉，且整个过程在同一帧内（看不到中间态）。实测：5 种落点（上边缘 / 行中间 / 下边缘 / 末行 / 列表下方空白）算出的插入行号全部符合预期；`move_items` 的单行下移、拖到首尾、多行同移、原地放下（上/下边缘都不改顺序）、无源行与越界都正确（另有 11 项离线测试锁住）。
- **拖动排序的「落点提示」与「界面比例被重置」（ROADMAP 74/75 配套）**：① **落点提示由「框住整行」改为「行间一条线」**：Qt 只在落点距该行上/下边界 2px 内才判 `AboveItem`/`BelowItem`（画线），落在行中间一律判 `OnItem`——而 `OnItem` 默认就是**画一个方框把整行框起来**，观感像「选中了这一行」。现用 `QProxyStyle`（`DropLineStyle`）把 `PE_IndicatorItemViewItemDrop` 统一画成一条 2px 的横线（`OnItem` 取矩形顶边、`Above`/`Below` 取 Qt 给的线位置，语义都是「插到这一行之前」，因此只改绘制、不动拖放行为），并且插入行号的判定（`drop_insert_row`）用的是**同一套阈值**，保证线与落点位置一致。离屏实测：`OnItem`（整行矩形）只在顶边画出 2px、**行内部无任何像素**（确认不再画框）；`Above`/`Below`（零高度位置）同样只出一条线；空矩形不抛异常。② **拖动后不再重置界面比例**：收尾原先会走 `_fit_table_height()` → `_apply_pane_ratio()` 按默认占比重新分配三个板块，把用户拖好的分隔条位置**重置**（用户反馈「拖动还会导致界面比例重置」）——而拖动一行并没有改变行数。现在重建行走 `_populate_rows(fit_height=False)`，只重填行、不碰布局；重建期间还屏蔽了选中信号，避免「清空 → 重填 → 重选」连着触发几次切换直播间，把 SC / 弹幕面板整段刷掉。另记一个坑：**合成的 `QDropEvent` 不能拿来做自动化测试**——喂给 `dropEvent()` 会让 Qt 访问违例（进程退出码 `0xC0000005`），因此收尾逻辑单独成 `_finish_drag()`，可以离线直接调用验证。
- **Qt 版房间列表交互：点击 / 切换选中会自动滚动（ROADMAP 74）**：Qt 默认的 `autoScroll` 会在 current 项变化时 `scrollTo` 把它**完全**显示出来，于是点一个只露出一半的行、或点横向被裁掉的单元格时，视图会自己跳一下。现关闭该行为，只在**拖动排序**（拖动期间临时开启，否则拖到列表边缘不会自动滚）与**键盘导航**（方向键移动 current 时跟随）期间恢复。Tk 版按 iid 读 tree、也没有 autoScroll，本就不受影响。（此前 `rowsMoved` + `room_order_from_rows` 那套顺序重建已被上一条的**自管拖放**取代。）
- **预览直播比网页端「越看越落后」（长时间观看落后数秒）**：实测（同一在播房间、经本机代理、FLV / HLS 各播 60 秒）播放器位置增长比墙上时间慢约 **0.16 / 0.07 秒每分钟**——这是播放端固有的（音视频同步与缓冲的微调），**与本地代理转发无关**；累积十几分钟就是数秒，与用户反馈一致。处理：① 代理转发由「攒够 64KB 再发」改为 `iter_any()`（上游一到数据就转出去）——这是我们唯一自己引入的固定延迟，顺手压到最小；② 新增**自动追边**：每路用「墙上时间增长 − 播放器位置增长」持续测量落后（纯函数 `needs_catch_up` 判定），累计超过阈值（新增配置 `preview.max_drift_sec`，**默认 3 秒**，写 `0` 关闭）就自动重新拉流跳到最新画面并提示「已落后 X 秒，正在追到最新画面…」，随后重建基线继续测；另有 20 秒起播宽限期与 60 秒最小追边间隔（防抖），只在播放中且未暂停时判定。实测（构造「已过 600 秒、位置只走 596.5 秒」的假状态）：落后 3.5 秒触发一次追边、2.0 秒不触发、暂停 / 关闭自动追边 / 起播 10 秒内 / 距上次追边仅 30 秒都不触发、追边后基线重建不会连续触发。**追边对画面的影响**（真实房间实测）：重新拉直链（0.1 秒）是在后台做的、**期间画面照常播放**；真正中断只有**重载那一下 0.44 秒（FLV）/ 0.73 秒（HLS）**——按默认阈值 3 秒与实测落后速度推算约**每 19 分钟闪一次**，远优于不追边（看 1 小时约落后 10 秒）。
- **多路预览的三处体验问题（P3 后续修复）**：① **切主界面的直播间会把预览的直播间换掉**——「跟随选中房间」原先在「只有一路」时会把这一路**换成**选中房间，于是在主界面点几下预览就被换走，也让人以为多开只能靠 Ctrl 多选。现改为**只切「主路」的声音**：选中的房间已在预览中→把它设为有声音的那一路；不在预览中→什么都不做（加减哪几路完全由用户决定）。配置说明与 `follow_room` 注释同步更新。② **加入预览不便**：新增**双击房间行**即加入/停止该房间的预览（跳转列除外，避免与单击打开浏览器打架），排序栏提示改为「拖动行排序，Ctrl 多选；双击行加入/停止预览」。③ **浮窗内关闭某一路会出现显示 BUG**：停掉**主路**时漏了一次布局重排（只有停副路的分支才调用 `_relayout`），于是被停掉的那一格**不会被隐藏**——窗口里留一块黑框、剩余格子也不重排。现无论停主路还是副路都重排（`_relayout` 里也补了「放大的那一路被停掉时自动退出放大」）。顺带补上 DD 监控室风格的**双击格子放大 / 还原**。
- **Qt 版启动后一闪就退出（P3 多路改造引入的回归）**：多路预览把工具栏「预览」按钮的文案改成按**选中的房间**决定（「预览」/「停止预览」），而这次刷新在 `_build_rooms_tab` 里被**提前**调用——那时房间表格 `self.table` 还没创建，`_get_selected_room_ids()` 直接抛 `AttributeError: 'QtScMonitorApp' object has no attribute 'table'`，界面构建中途异常、进程随即退出（用户看到的就是「启动后只闪一下」）。原实现只读 `self._selected_room_id`（`__init__` 早期就已赋值）所以一直没暴露。现让选中查询容忍表格尚未创建（`getattr(self, "table", None)` → 返回空列表，按「无选中」处理），并补一条静态检查锁住这条启动保护。实测：无房间 / 1 个房间两种配置都能正常启动（880×900，「预览」按钮在未选中房间时置灰）。
- **Qt 版点击弹幕复制正文：部分弹幕点了没反应、点空白也会复制**：① 点击当时只认「正文段带 dmid」的弹幕，而 dmid 未必都能从报文取到（`_extract_dmid` 取不到时为空字符串），这类弹幕的正文档没有任何可点击标记——点上去毫无反应（既不复制也没有提示）。现给正文段统一加 `_DM_BODY_KEY` 标记（与 dmid 无关），点正文即复制：优先用 dmid 元数据里的原文，取不到再从该行文本还原（与 Tk 版 `dmbody` tag 的做法对齐）。② 命中判定原先只看「`cursorForPosition` 落点字符是不是空白」，但 Qt 会把坐标**吸附到最近的字符**：行尾右侧与文本区下方的大片空白都会被吸附到最近的字符上，于是点空白也复制（悬浮表情提示同样会误弹）。现改为带几何约束的 `_clicked_char`：垂直方向必须落在该行行高内、水平方向不得超过该行文本右边界；点到字符右半边时 `cursorForPosition` 指向的是下一个字符（行尾即换行符），此时回退一位——否则「点弹幕最后几个字复制不出来」。悬停（表情提示）与右键菜单复用同一套判定。实测：正文任意位置可复制、缺 dmid 的弹幕可复制、行尾右侧空白 / 文本下方空白 / 时间戳均不触发。
- **Qt 版窗口尺寸一直没真正生效（窗口偏矮）**：`_apply_window_geometry()` 原先在 `_build_ui()` **开头**调用，那时标签页 / 表格 / 分隔条都还没建，随后添加的控件会把刚设好的尺寸顶回布局 sizeHint——实测启动后窗口只有 **880×600**，`DEFAULT_WINDOW_SIZE` 与「上次退出记住的尺寸」都看不出效果（退出时仍在记，所以记忆值一直在攒）。现改为**界面建完之后**设置（`__init__` 里 `_build_ui()` 之后）。同时把默认窗口高度 **820 → 900**（1440p / 150% 缩放下可用高度约 912，一屏放得下 SC 与弹幕区），屏幕上限由「可用高度的 92%」放宽为「可用高度 − 12px」；Tk 版默认高度 **680 → 760**（DPI 缩放后按屏高兜底，小屏不会顶出屏幕之外）。实测：无记忆时 1080×900；记忆为 1080×834 / 1080×900 时分别恢复为原值。
- **Qt 版页签外观（上一版界面优化引入的回归）**：紧凑样式里写了 `QTabBar::tab { padding }`，一旦样式表涉及页签，Qt 就**放弃 Windows 原生页签绘制**改用样式表风格，选中页与未选中页几乎看不出区别（表现为「像是当前页**左侧**那一页被按下」）。已移除该规则，页签交回系统原生绘制，其余控件的紧凑内边距保留（常量注释里也写明「不要再覆盖 QTabBar」的缘由）。
- **Qt 版房间列表高度上限**：列表按行数收缩后仍会继续长高（上限是分隔区的 55%），房间一多就把 SC / 弹幕区挤成一条缝；现限制为**初始最多 7 行**（`TABLE_MAX_VISIBLE_ROWS`，超出走滚动条），并补上表格边框与 4px 余量——此前余量不足会导致「最后一行只显示一半」。实测：1 / 2 / 6 行时按内容显示且不滚动，8 / 20 行时为 7 行高度 + 滚动条（SC 区仍保留约 475px）。
- **Qt 版房间列表高度被锁死（上一版引入）**：用 `setFixedHeight` 设表格高度会连带把面板的**最小**高度一起钉住，QSplitter 因此再也压不下去（实测想分配 160px、实际仍是 216px），用户无法把列表拖小，「至少 7 行」成了硬下限。现改为**只通过分隔条分配初始高度**（新增 `_table_preferred_height` / `_room_pane_preferred_height`，表格的 min/max 尺寸复位为可伸缩），列表随时可以拖动改变：实测可压到约 103px（≈ 2 行）、也可拖到 300px 以上。
- **高频开播（下播几秒后又开播）时状态与开播提醒会丢（ROADMAP 62）**：状态更新的两个方向原先并不对称——关播侧有「推送置 0 + 延迟确认 + 接口缓存纠正」，开播侧却只有「收到 `LIVE` → 拉一次接口」，而**开播瞬间 `get_info` 同样可能仍返回缓存的 `live_status=0`**，这次刷新就把开播覆盖成了未开播；此后不再有任何复核（60 秒的只读轮询只覆盖**没有弹幕连接**的房间），状态因此**永久卡在「未开播」**——开播提醒不触发、依赖开播的自动点赞也不动。实测复现：下播 → 2 秒后又开播（接口 0.6 秒缓存窗口）→ 旧逻辑状态序列 `[1, 0, 0]`、开播提醒 0 次。另外关播信号此前**没有过期时间**，一旦开播推送丢失（断线期间重开播、服务端未推），它会**永久压制**接口返回的开播状态。现改为：① **开播推送立即置为直播中**（推送是权威信号），随后由防抖刷新补齐标题/uid；② 开播也纳入**保护窗口**（`PUSH_STATUS_TTL`，默认 30 秒）——窗口内接口返回相反的缓存值一律以推送为准，**关播信号同样受该窗口约束**，过期后以接口为准，不再被一次丢失的推送永久带偏；③ 新增**周期状态复核**（每房间约 5 分钟、带 ±30% 抖动避免多房间撞点），兜底修正丢失的推送，状态可自愈；④ 重复的开播推送不再重复广播（避免重复提醒）。修复后同一时序状态序列 `[1, 0, 1, 1]`、开播提醒恰好 1 次、自动点赞触发 1 次；推送丢失场景也能在信号过期后恢复为直播中。
- **同一房间「自动发弹幕」与「自动点赞」都开时，点赞长期轮不到（Tk / Qt 两版均存在）**：周期轮询里两项是**各自独立提交**的（Tk 用两个 `if`、Qt 用 `if/elif`），而先提交的那一项会让房间**立即**进入「该房间正在执行中」，后一项随即被挡掉；下一轮又重复同样顺序——于是只要发弹幕可执行，点赞就永远不执行，只剩开播瞬间那一次即时触发（若程序启动时房间已在直播、没有开播跳变，点赞要等到下一次开播）。现把「本轮该执行哪些」抽成两版**共用的纯函数** `medal_tasks.auto_task_types()`，把该房间本轮要做的项**汇总成一次提交**（执行引擎本就支持 `only=[...]` 在同一次执行里依次完成）。顺带：未开播时不再提交必然被跳过的点赞（省掉每轮 2 次接口请求）、未持有粉丝牌的房间静默跳过（Tk 此前每轮白跑一趟并弹提示）、两版决策经 8 个场景逐一比对完全一致。
- **「开播时也自动发弹幕」文案统一为「允许开播时自动发弹幕」**：该项是**附加许可**——默认关 = 仅未开播时自动发弹幕，勾选 = 开播（直播中）时也发。旧文案（Tk「开播时也自动发弹幕」、Qt 更早还写成「仅开播时自动发弹幕」，字面正好读反）容易与默认行为混淆，现两版统一为「**允许开播时自动发弹幕**」，Qt 另补悬浮说明（含「需先勾选『自动发弹幕』才生效」「手动按钮不受限」）。
- **Qt 版粉丝牌页勾选框文案说反（「仅开播时自动发弹幕」→「允许开播时自动发弹幕」）**：该项是**附加许可**——不勾（默认）时**只在未开播时**自动发弹幕，勾选后才**额外允许开播（直播中）时**也发；原文案「仅开播时自动发弹幕」字面正好说反（会被读成「只在开播时发」），而该项默认是**不勾**的，极易误操作。现改为「**允许开播时自动发弹幕**」（与提示文案「已为该房间允许/禁止在开播时自动发弹幕」口径一致），并给两个发弹幕勾选框补**悬浮说明**：「自动发弹幕」标注默认只在未开播时执行、「允许开播时自动发弹幕」标注需先勾选前者才生效、手动「发弹幕」按钮不受此限制。逻辑本身经核对无误：判定集中在 `should_auto_danmaku(直播中, 该项)`（未开播恒执行、开播需该项为真），仅用于**自动**任务的周期检查；手动按钮走 `only=TASK_SEND_DANMAKU`，完全不读该开关。
- **Qt 版粉丝牌页「自动」勾选框不随所选房间更新（并连带使「发弹幕 / 点赞」按钮与自动任务全部失效）**：`MedalTab.runner()` 构造执行器时用了宿主上**不存在**的属性名 `host._medal_emit`（Qt 宿主实际叫 `_medal_event`，只有 Tk 版叫 `_medal_emit`），因此只要后台已就绪（`hub.api` 非空）就会抛 `AttributeError`；它发生在 `itemSelectionChanged` 槽里 `refresh_auto_checks()` **之前**，异常被 Qt 吞掉（`pythonw` 无控制台，调试页也看不到），表现为切房间时下方三个勾选框与按钮可用状态**一直停在上一个房间的取值**，同时「发弹幕 / 点赞」按钮点了没反应、每 5 分钟的自动任务周期直接中断（自动任务因此从未真正跑起来）。现改用 `host._medal_event`，并调整 `_on_task_selected()` 的顺序：**先做纯界面同步**（勾选框取值 + 可用状态），再查「是否正在执行」，且执行器异常只记日志（含堆栈、调试页可见）而不再连带冻结界面。另新增 `tests/test_qt_wiring.py`：静态交叉检查 Qt 子模块访问的 `host.<name>`、以及宿主访问的 `self.medal_tab / dm_panel / overlay.<name>` 是否真实存在，让这类**只在运行期、且在槽函数里静默**的接线错误在测试阶段就能暴露。
- **老版本 `config.json` 缺少后来新增的配置项（如 `medal_tasks` 看不到、也无从开启）**：默认配置模板**只在文件不存在时**生成，因此升级前就已存在的 `config.json` 永远不会获得新字段——用户既看不到 `medal_tasks.auto` 这个「全自动」总开关，也无法开启它（代码按 fail-safe 当 `false` 处理，自动任务静默不生效）。现改为**读取时就地补全**：只补缺失的键、**绝不覆盖已有值**（用户改过的 `true/false` 一律保留）、**空对象不填充**（`emoticon_tooltip: {}` 这类「显式关闭」的写法不会被补回默认开关），用户自定义的额外键也保留；补全后顺带把文件内自动生成的 `_说明` 刷新为最新版本（旧说明会漏讲新项），并在调试日志里提示已补全。配置已完整时不再改写文件，损坏或非 JSON 对象的文件仍不做任何改动。
- **Qt 版缺少「点击保护」**：Tk 版点击列表里的**跳转列**（房间号 / 主播名 / 提醒列）时不会改变选中行，因此点主播名打开个人空间**不会连带切换**下方的 SC / 弹幕面板；Qt 版原先用 `cellClicked`，而选中行在**按下时就已经改变**——不仅会切走面板，粉丝牌任务表里点主播名还会顺手改掉「要执行任务的房间」。现新增 `ProtectedLinkTable`：重写 `selectionCommand`，让跳转列的鼠标**按下与松开都返回 `NoUpdate`**（Qt 在「按下未改变选中」时会在**松开时补一次选中**，只拦按下会导致保护在松手瞬间失效），松手且未拖动到其它单元格时才发出 `linkClicked` 完成跳转；普通列照常选中、**行拖动排序也不受影响**。三张表分别配置：房间列表（房间号/主播/提醒）、粉丝牌任务表（房间号/主播）、我持有的粉丝牌（主播/房间号）。
- **Qt 版行间距偏大（SC 每条记录之间多一整行空行 + 表格行高过松）**：① `build_sc_segments` 末尾本就带 `\n`，而 Qt 的 `insertText("\n")` 已经会另起一个文本块，代码随后又调了一次 `insertBlock()`，于是**每条醒目留言后面都多出一整行空行**（行距翻倍）；实时追加与向上翻页插入两处都删掉多余换行，现在每条 SC 独占一行、与 Tk 版一致。② **表格行高**改为「字体行高 + 8px」：Qt 默认行内边距约 18px（实测 30px 行高 / 12px 字体行高），中文列表显得很空；房间列表、我持有的粉丝牌、监听房间任务三张表统一收紧，并随 DPI 缩放自动变化。③ **弹幕区字号**从 10pt 调回 **9pt**（与 Tk 版一致，SC 区仍为 10pt），弹幕行距同步变紧。
- **被「被占用」或「已停用监听」的直播间不显示主播名与直播标题**：这两类房间不会建立弹幕连接，也就收不到弹幕推送的 `status` 事件，列表里的「主播」「直播标题」一直是空的——同一个房间被另一个实例（另一个 GUI 窗口或命令行）监听时尤其明显：用户只看到「被占用」，却不知道是哪个主播、在播什么。现在为**没有弹幕连接的房间**补一条**只读 HTTP 通道**：命中「已被其它实例占用」时、程序启动时即处于「停用监听」的房间、以及此后每 **60 秒**一轮，用 `get_full_room_info` + `get_anchor_name` 填充主播名 / 直播标题 / 直播状态（顺带回写主播 uid）。**有弹幕连接时直播状态仍以弹幕推送为准**（客户端对「关播瞬间接口返回缓存旧状态」有专门处理，不被只读查询覆盖）。Tk 版与 Qt 版同步实现。
- **Qt 版补齐 Tk 剩余的界面功能（逐项方法比对后收口）**：① **点击 SC 用户名可跳个人空间**（此前 uid 属性只写不读）；② **点击弹幕正文真正复制到剪贴板**（此前只弹「已复制」提示、并未写入系统剪贴板），并把「已复制」拆到独立提示行，不再覆盖发送状态文案；③ **中键浏览器式快速滚动**（按一下中键进入、鼠标越远滚得越快、再按中键或单击左键退出），SC 区 / 调试日志 / 弹幕区三处均可用；④ **房间表列宽保护**（直播标题为弹性列 + 最小列宽，拖动列宽时弹性列让位，不会把列挤出窗口）与**状态排序补「轮播中」**；⑤ **备注写回所属房间**（此前用「当前选中房间」，切换行的瞬间会把 A 房间的备注写进 B 房间）；⑥ **粉丝牌执行期实时进度**（`medal_note`/`medal_start`/`medal_progress`/`medal_task_progress` 四类事件此前只认一个不存在的事件名、全部丢失），并补页签状态行「写操作：启用/关闭 · 全自动总开关：开/关」、任务结束后自动补刷粉丝牌与该房间任务、自动点赞前「未持有粉丝牌」跳过判断；⑦ **表情条被压扁而非横向滚动**（`QScrollArea` 开启 `widgetResizable` 会把单行内容缩到视口宽度），现按各表情按钮自然宽度设置最小宽度，窄时铺满、宽时横向滚动，并支持**滚轮横向翻动**；⑧ **三板块默认占比**改为按 Tk 的 `PANE_RATIO`（2:3.5:4.5）计算，弹幕区显隐时重新分配；⑨ 调试日志改为**批量插入**（同 Tk 版做法），减少重排开销。
- **Qt 版弹幕表情悬浮看不到原图**：Tk 版悬浮弹幕里的表情会在提示窗显示原图，Qt 版此前只有文字。现补齐整条链路：表情弹幕记录 `emoticon_unique → {触发词, 数字 id, 房间, 图片地址}`（**加 400 条上限**，此前无上限会持续增长）；报文不带图片地址时用**已缓存的房间表情包兜底**补地址与 id，表情包尚未加载则后台拉取一次（同房间 30 秒冷却）；悬浮命中表情时**懒加载原图，图就绪后自动回填到正在显示的提示窗**（未就绪时先显示 `config.json` 的 `emoticon_tooltip` 文案）；提示窗改为「**有原图只显示图**（触发词与弹幕内容重复）、无原图显示文案」，定位改用**虚拟桌面并集**做越界回缩，多显示器（含副屏在主屏上方/左侧）也不会跑出屏幕。顺带修掉两个遗留问题：默认配置下触发词在提示里**重复显示两遍**（`text` 字段被额外拼接了一次）；表情面板按钮的悬浮提示写死了非法字段名 `trigger`，导致只显示 `unique/id` 而**看不到触发词**，现按 `config.json` 的 `emoticon_tooltip` 渲染。
- **「被监听房间显示为被占用」时无从排查**：同一房间不会被两个实例重复监听（房间级互斥锁），但界面此前只显示「被占用」，用户看不出是谁在占用、也不知如何恢复。现新增 `read_room_lock_holder()` 读取锁文件里记录的持有者，**两版 GUI 都会把「房间 X 已被其它实例监听（持有者 pid=… started=…）」写进日志**（Qt 版可在「调试」页查看），Qt 版还在状态格上挂了悬浮提示，直接说明「关闭对应窗口/进程后重启本程序即可恢复」。
- **拖动窗口或分隔条后看不到最新内容**：SC 区与弹幕区原本贴底时，视口尺寸变化后 Qt 只保留滚动条数值，内容因此停在旧位置（最新消息被推到视野外）。现新增 `BottomHoldTextEdit`：尺寸变化时若「变化前位于底部」则自动重新贴底，**用户正在向上翻阅历史时不受影响**；吸底状态由 100ms 轮询维护，行为与 Tk 版一致。
- **Qt 版（`qt_gui.py`）与 Tk 版功能未真正对等（一批缺陷）**：`QT_PORTING.md` 此前的「48/48 ✅」与实际实现不符，经逐项比对 Tk 源码后修复以下问题——① **弹幕收不到/不落盘**：`RoomClient` 的弹幕接收开关默认关闭，Qt 版从未调用 `set_danmaku_enabled`，现补门控（开关开启且房间被选中才接收），并在启停监听、切换房间、开关弹幕区时同步；② **弹幕行裁剪崩溃**：`dm_trim_index()` 返回的是 Tk 风格行号字符串，被直接传给 `findBlockByNumber(int)`，弹幕超过 4000 行即抛 `TypeError`，现已转为整数；③ **表情面板打不开**：Qt 版缺少发送行里的「表情」按钮，现已补上（并可 Esc 收起），同时补齐表情图片下载/缩放/缓存与按房间记忆停留的表情包，「发送表情」也改为按 `emoticon_unique` 提交（原先发的是触发词，会被服务端拒绝）；④ **调试日志页永远空白**：Qt 版没有安装日志收集器，现补 `_QueueLogHandler` 与 Formatter，日志级别改根 logger，超 4000 行裁掉最旧一半；⑤ **粉丝牌任务全部取不到**：查询接口要的是主播 uid 而 Qt 版传了房间号，且「发弹幕」列用错了 `jump_type`（`danmaku` → `sendDanmu`），另补「自动」列、任务表行序跟随直播间列表、启动即拉取粉丝牌与任务、停留页签 90 秒静默刷新；⑥ **自动任务绕过总开关且频率过高**：Qt 版只判 `allow_write_operations`，漏判 `medal_tasks.auto`，且周期为 5 秒（Tk 为 5 分钟），现均按 Tk 口径修正；⑦ **直播标题列显示的是直播状态**：Qt 版从未保存标题，现独立缓存标题、状态列按连接态给出「已停用/连接中…/被占用/已停止」；⑧ **音效选择不生效**：下拉未接信号、试听播的是已保存值，现选择即保存并试听；⑨ **弹幕开关偏好键不一致**（`show_danmaku` → `dm_visible`，与 Tk 共用同一份配置）；⑩ **切房间不清空弹幕区**、**回复弹幕未带 `replay_dmid`**、**服务端可用弹幕颜色/模式未按房间刷新**、**退出时不等后台落盘**等问题一并修正。
- **粉丝牌进度不会自动更新**：任务执行期间界面一直停在旧进度，且「我持有的粉丝牌」列表（今日亲密度 / 当前经验 / 状态）只在手动刷新时更新。现补齐自动刷新——① 执行引擎**每轮复核后实时上报进度**，界面就地更新对应行的单元格；② 任务结束（手动/自动）后自动刷新粉丝牌列表与任务；③ 停留在「粉丝牌」页签时每约 **90 秒**静默刷新（粉丝牌列表 + 各房间任务），切到其它页签即停止。
- **点赞任务失败（重试轮数不足）**：实测服务端对点赞计数**有节流/延迟**——单次点赞请求通常只推进约 1 点进度，`click_time` 大小不保证等量计入。原实现把总尝试次数限制为 `max_retry+1`（默认 4），进度到不了上限（如 10）就判「未完成/失败」。现改为**按随机间隔多轮发送直到进度达上限**（完成即停），并把 `medal_max_retry` 语义改为「**连续无进展/失败上限**」（达到后停止本轮，留待下次或自动任务下一轮继续，不无限空发）；`click_time` 改取任务标题里的次数（如「点赞30次」→30，对齐网页端）。发弹幕同样改为按轮次推进。真实账号验证：房间 1700301235 点赞 5 轮后 5/10→10/10，用时 38.6s。
- **未点亮的粉丝牌导致点赞/发弹幕任务「失败」**：实测这类粉丝牌的任务 `sub_title` 为「**仅点亮**」而非「当前/上限」，解析出的上限为 0；原实现把它当成待办，又因「剩余量」为 0 而回退成默认 `click_time=30`，于是**空发大量点赞/弹幕请求**，直到重试耗尽才报「任务未完成/失败」。现新增 `is_task_applicable`（上限为 0 且未完成视为**不适用**），这类房间**直接跳过、不发任何请求**并提示「粉丝牌未点亮（任务显示「仅点亮」），暂无可执行任务」；界面任务格显示「仅点亮」而非「0/0」。（已用真实账号验证：未点亮房间耗时 0.1s 返回、零请求）
- **存在未持有粉丝灯牌的直播间时，任务刷新一直停留在「正在刷新」**：批量刷新结束时补充汇总上报（「任务刷新完成（N 个房间）；有任务 x；无粉丝牌 y；失败 z」），不再卡住；且**已知未持有粉丝牌的房间直接标记「无粉丝牌」并跳过接口请求**，点击「一键完成」/自动任务时也会跳过并提示。
- **自动点赞不停在首个周期（开播了却不自动点点赞）**：自动点赞此前仅靠每约 5 分钟的周期轮询，且要求轮询那一刻该房间**已处于直播中**才执行——若开播发生在两次轮询之间，就要空等最多一个周期（几乎等于不生效）。现已改为在**收到开播信号时立即触发**自动点赞（见「新增」），周期轮询只作兜底；开播与轮询共用同一触发判断，正在执行的任务不会被重复触发。
- **粉丝牌页「今日亲密度」数值含义易误解**：修正为上面「今日/升级需」的明确语义（原实现虽取同一字段，但列名与格式易被误解为「当前/升级」）。

> ⚠️ 自动点赞/发弹幕属高风险操作，可能触发风控甚至账号处罚，是本项目「不自动发送、不自动重试」原则的显式例外。请自行评估后谨慎开启。

## [1.8.0] - 2026-09-12

### 新增
- **发送直播间专属表情包**：弹幕发送行新增「表情」按钮，点击后在**发送行上方内嵌展开**该直播间的表情面板（再点一次、点面板内「收起」或按 Esc 收起；切换直播间自动收起；**不另开独立窗口**）——**按表情包分页（一页 = 一个表情包，顺序与服务端返回一致，与直播间内面板的分页逻辑相同）**，顶部可「◀ 上一包 / 下一包 ▶」或直接从下拉框跳到某个包；**表情条固定单行展示、面板高度恒定**（约 155px，**不随表情数量或图标大小变化**），一行放不下时**横向滚动**（横向滚动条常显，滚轮也可横向翻动；一页表情较少时各列等宽等分余量，两侧留白对称）；表情取自服务端 `GetEmoticons` 接口的**实际可用项**（`perm=1`，跨包按 `emoticon_unique` 去重，剔除空包），**点击即发送**（与网页端一致，按 `dm_type=1` 的表情包弹幕发出，`msg` 取 `emoticon_unique`——见下方修复条目）；表情图片按**原始大小 1:1** 显示、不做缩放并留出间距（后台限流 4 并发下载、按 url 缓存），图片格式不受 Tk 支持时（如 WebP）自动回退为「[触发词]」文字按钮，接口失败时显示明确空态而不影响其他功能；发送后面板保持展开，可连续挑选多个表情；**按直播间分别记住浏览到的表情包**（**翻到哪一页就写入** `gui_rooms.json` 的 `emoticon` 段，**重启后仍生效**；其他保存动作不会把它抹掉，删除直播间时一并清理），再次展开直接回到该包（优先按**包名**找回，表情包顺序变化也不跑偏；列表变化时自动收敛到有效序号）；**每次展开都会后台刷新可用表情包**（同一房间 30 秒冷却），因此**粉丝灯牌升级等新解锁的表情包下次展开即可同步**——刷新落地时若表情包有增删，会按包 ID/名称停在原来那个包（位置前后移动也不跳走），内容没变则不重绘（保住横向滚动位置），刷新失败时保留上次结果不清空。属**写操作**，同样受 `config.json` 的 `allow_write_operations` 总开关与登录态约束，并沿用同一房间 2 秒冷却与重复内容拦截；发送失败按 B 站返回码给出中文提示，不自动重试
- **点击弹幕正文复制**：单击弹幕**用户名**仍跳转其个人空间；单击**正文文字**则把该条弹幕文本复制到系统剪贴板，并在发送区下方短暂显示「已复制：…」（1.5 秒后自动清除，与发送状态提示互不覆盖）；时间、用户名、以及正文右侧的空白/行尾区域都不会触发复制（正文标签只覆盖文字本身，行尾换行另挂标记）
- **新消息数量提示**：SC 区与弹幕区的滚动条不在最底部（正在翻阅历史）时，新到达的内容不再静默沉底，而是在该区域**右下角浮出可点击徽标**「N 条新SC ↓」「N 条新弹幕 ↓」；点击徽标即回到底部并恢复自动跟随；用户手动滚回底部、切换直播间或清空视图时计数自动清零、徽标隐藏
- **弹幕里的表情包显示**：整条都是表情的弹幕（`dm_type=1` 的大表情/粉丝团表情）在弹幕区显示为「[触发词]」并带标记，**鼠标悬浮 0.5 秒即可在提示窗里看到表情原图**（图文并排；原图懒加载，未缓存时先显示文字、就绪后自动补上）。弹幕流内**不做内嵌图片**——压力实测（独立压测程序）：直播间表情弹幕占比常超过 50%，内嵌图片与对缓冲区前部的回退编辑都会随历史弹幕量把 Tk 卡死；悬浮显示不进文本流，**点击复制、右键回复、悬浮提示字段**（`config.json` 的 `emoticon_tooltip`）均不受影响。**落盘文件仍写 `[触发词]` 文本**，`dm_YYYYMMDD.jsonl` 格式不变
- **鼠标悬浮显示表情标识**：悬浮在**表情面板的表情**或**弹幕区的表情图**上约 0.5 秒，弹出不抢焦点、不进任务栏的小提示（复用悬浮窗的 `WS_EX_NOACTIVATE` 手法）；显示哪些字段由 `config.json` 的 `emoticon_tooltip` 控制——支持列表 `["text","unique","id"]` 或开关对象 `{"text": true, ...}`，**默认仅触发词**（`[百岁山]`），可另加 `unique`（`room_9527_109824`）与数字 `id`；给空列表/全 `false` 表示关闭提示，字段名写错则回退默认。鼠标移开、表情换页、收起面板、点击弹幕、切换直播间与退出程序时都会立即隐藏；弹幕区按光标下的表情**去抖**，同一表情内移动不会反复重排

### 优化
- **弹幕发送去掉三项客户端限制**：取消 **20 字长度校验**、**30 秒重复内容拦截**与**发送前二次确认弹窗**——输入后回车/点「发送」直接发出，表情面板点击即发不变。仍保留：`allow_write_operations` 写操作总开关与登录态门控、同房间 **2 秒发送冷却**、发送中防连点、空内容拦截；「0/20」计数保留仅供提示，长度与重复由**服务端**判定并按返回码给出中文提示（如超长 `1003212`）。程序仍**不会自动发送、不会自动重试**
- **表情悬浮提示定位更准**：定位时机从「悬浮开始时记录坐标」改为「**显示瞬间**读取当前光标位置」——0.5 秒延时期间鼠标已移开时，不再弹在旧位置上（显示后不跟随，内容去抖逻辑不变）；悬浮到弹幕表情上时**懒加载原图**，就绪后正在展示的提示窗自动补上图片
- **SC/弹幕区吸底**：原本位于最底部时，拖动窗口、拖动分隔条、展开或收起表情面板后，视口自动保持吸底（去抖 80ms 处理，避免拖动过程中频繁重排）；用户向上翻阅历史时视口不受影响，与新消息徽标逻辑互不干扰
- **断线重连加固（短时断网更快恢复）**：
  - 重连退避改为**指数退避 + ±30% 随机抖动**，上限由 60 秒收紧到 30 秒，避免多个直播间在同一时刻同步重连形成尖峰；
  - 心跳发送间隔 30→20 秒、应答超时 70→45 秒，并把「超时判定」与「发包节拍」解耦为 5 秒轮询——**静默断网（连接未关闭但不再有数据，如拔网线）的发现时间由最长约 100 秒缩短到约 50 秒**；
  - 断线与恢复过程输出可观测日志：`连接中断: …` → `N.N 秒后重连（第 n 次）` → `已接入…` / `断线后已自动重连成功（第 n 次尝试）`，可直接据此确认网络波动后是否及时恢复

### 优化
- **点击「提醒」开关不再切换直播间**：直播间列表的「提醒」列单击切换开播提醒开关时，不再连带把下方 SC/弹幕面板切到该房间（与点击房间号/主播名跳转时的处理一致）
- **config.json 移出仓库，运行时自动创建**：`config.json` 含个人写操作开关，不再随仓库分发并已加入 `.gitignore`；**首次运行程序**（GUI 或命令行）时若文件不存在会自动生成带字段说明的默认配置（写操作关闭），生成失败不影响本次运行，损坏的配置按默认处理且不会被覆盖

### 修复
- **部分直播间的表情面板全是文字按钮（无图）**：实测（房间 1773209737）这类房间的房间专属表情图片是 **WebP 格式**，Tk 原生只支持 PNG/GIF，解码失败后回退为文字。现接入 **Pillow**（新增依赖）自动把 WebP 等格式转成 PNG 再显示，未安装 Pillow 时维持文字回退；同时表情图片下载请求补上 `Referer`/`Origin` 头，规避部分 CDN 资源的防盗链 403
- **表情悬浮提示的三处问题**：
  - 无图提示会**残留上一次显示的原图**——Tk Label 未显式清除 image，切到其他表情时旧图仍在；现在每次显示都按「有无原图」重设图文；
  - 显示原图时**触发词仍与图并排**——按场景区分：**弹幕区**有原图时只显示图（触发词与弹幕内容重复）；**表情面板**只显示触发词（按钮本身已是图，触发词正是需要的信息）；
  - **部分直播间的表情只显示触发词、不出原图**——这些房间的弹幕报文不带图片地址。现改为：优先用报文地址 → 缺失时用**已加载的表情包**兜底补地址 → 表情包也未加载则在悬浮时后台拉取一次（同一房间 30 秒冷却），地址就绪后提示窗自动补上原图
- **长时间挂在直播间后界面卡死**：弹幕区的文本行数**没有任何上限**，热门直播间挂几个小时可积累十几万行（每行还挂着 3~4 个标签），Tk 的插入/滚动开销随行数不断上涨，最终把整个界面卡死。现给弹幕区加 **4000 行上限**：超限自动裁掉最旧的一半（保留最近 2000 行），插入耗时恢复平稳（实测第 1~1000 条平均 3.8ms/条、第 5000 条后 4.8ms/条，不再随行数增长）；**落盘文件不受影响**，完整弹幕仍按日保存在 `dm_YYYYMMDD.jsonl`。SC 区为分页加载且条目少，不加上限
- **表情悬浮提示被拽回主屏（多显示器）**：程序放在副屏时，悬浮提示只在主屏出现——定位用的是 `winfo_screenwidth/height`（**只反映主显示器**，实测主屏 1707×960），光标在副屏时被硬夹回主屏右缘。现改用 `winfo_vroot*` 提供的**整个虚拟桌面**（多显示器合并区域，实测 3640×1920）做越界回缩，提示跟随光标出现在所在显示器上；仅在虚拟桌面右/下边缘才向内回缩。已知限制：Windows 下 Tk 无法用绝对负坐标定位，副屏在主屏**左侧**（虚拟桌面原点为负）时提示会落在主屏左缘，不会跑出屏幕
- **表情包发送被服务端拒绝（`code=10203`）**：原先表情包弹幕的 `msg` 传的是表情触发词（如 `百岁山`），服务端不接受该形式。改为按实测可用方案发送——**`msg` 传表情唯一标识 `emoticon_unique`**（即网页端 DOM 的 `data-file-id`，形如 `room_<房间号>_<id>`），`emoticonOptions` 改为携带 `emoticon_unique` / `url` / `width` / `height` / `in_player_area` / `bulge_display` 的 JSON 对象（宽高缺失时回退 40）。`10203` 已加入返回码中文提示表，再次遇到时给出明确原因说明；普通文字弹幕的 `msg` 行为不变
- **「大表情」被压得过小、糊成一团**：Tk 的 `PhotoImage` 只支持整数倍 `zoom`/`subsample`，原先按最长边取整缩小，把 162×60 的房间专属表情压成 **54×20**（231×60 甚至只有 57×15）。现改为**按原始大小 1:1 显示、完全不做缩放**——这些表情本来就是 60px 高上下的小图，1:1 最清晰（132×60 与 162×60 不再缩水，231×60 也完整显示）；只有超过 **260×72** 的异常大图才等比缩小，且用 `zoom(a)+subsample(b)` 近似小数比例（而非粗暴的整数倍 subsample），不会被再次压糊
- **退出程序时未记住表情包**：原先只在「收起面板」时写盘，而最常见的路径——面板一直开着直接关窗口退出——就丢失记忆。现改为**翻到哪一页即写入**，并在「退出确认后」与「窗口销毁前」各补一次（内部比较无变化时跳过写盘，不产生多余写入）；另外关闭「弹幕」开关导致整块弹幕区（含表情面板）不可见时，也会顺手收起并记录
- **表情面板右侧留空、高度偏高**：原先按固定格子宽度推算列数与行数，网格单元宽度没有算上按钮自身内边距（列数不准、最右一列外侧留白），且面板高度随表情数量在 130~280px 之间浮动。现改为**固定单行 + 横向滚动**：面板高度恒为约 155px，一页表情再多也不换行、不加高；一页表情较少时各列等宽等分余量，图标在列内居中、两侧留白对称。同时正常浏览时不再显示底部提示行（操作提示移到面板标题「发送表情包（点击即发送）」）
- **弹幕区表情图导致界面卡死（多轮排查）**：内嵌表情图在直播间场景下不可行——表情弹幕占比常超过 50%，内嵌图窗口随历史弹幕积累（数千个）会让 Tk 逐渐卡死；即使限制内嵌数量，每次图片降级都要改写缓冲区前部、使其后所有行的布局缓存失效，卡顿仍随表情速率与历史量增长。压力实测确认后**移除弹幕流内嵌图片与「表情图」开关**，改为悬浮显示原图（见上方「新增」条目）；表情面板、落盘、复制/回复均不受影响

### 测试
- 新增 64 个单元测试：新消息徽标文案、弹幕正文还原（剔时间/用户名前缀）、重连退避（指数增长/封顶/抖动上下界）、`GetEmoticons` 响应解析（按表情包分组、保持服务端顺序、`perm` 过滤、跨包去重、剔除空包、兼容扁平形态）与失败降级返回空、表情包发送表单字段（**`msg=emoticon_unique`**、`dm_type`、`emoticonOptions` 各项及其缺省值、`10203` 提示）、表情缩放（**真实尺寸按原始大小不缩放**、超行高才缩小、小数比例近似、不超上限/不放大、非法元数据）、表情包指纹（一致则判定无需重绘、能识别新解锁的包/包内新表情、容错非法输入）、表情包记忆持久化（写入/读回、其他保存动作不丢、空记忆不写键、缺文件/损坏/字段非法时的容错）、**弹幕表情对象提取（真实结构、缺字段/脏数据）与 payload 附带 emoticon**、**`emoticon_tooltip` 解析（列表/对象两种写法、显式关闭、非法回退默认）**、**悬浮提示文案拼接（默认仅触发词、字段顺序、缺值跳过、空配置）**、**按 unique 补数字 id**、**提示窗定位（越界回缩、不出屏）**、弹幕表情图开关默认值与读写、心跳超时被及时判定

## [1.7.0] - 2026-09-11

### 新增
- **应用级配置文件 `config.json`**：项目根目录新增配置，含写操作总开关 `allow_write_operations`（发送弹幕等向 B 站提交数据的操作）。**默认关闭**，程序默认保持「只读」；仅在文件中显式改为 `true` 后相关功能才启用，修改后需重启生效。字段缺失、文件损坏或类型非法（如字符串 `"true"`）一律按关闭处理（fail-safe），启动时在调试日志显示当前状态
- **发送弹幕（GUI）**：弹幕区底部新增发送行（内容 + 颜色 + 展示模式），支持**右键弹幕「回复该弹幕 / @ 该用户」**；同一房间 2 秒冷却、短时间重复内容拦截、发送前二次确认；失败按 B 站返回码给出中文提示（未登录 / csrf 失败 / 超长 / 频率过快 / 风控）。**属于写操作，受 `config.json` 的 `allow_write_operations` 总开关约束（默认关闭）**，且需已登录 Cookie（含 `bili_jct`）；不自动发送、不自动重试
- **房间操作就近化**：把「备注 / 开启·停用监听 / 删除」从窗口最底部移到**直播间列表正下方**，窗口底部仅保留「刷新历史 / 获取Cookie」；房间列表可见行数随之自动校正，不遮挡列表

### 修复
- **取消勾选「弹幕」后底部勾选框/按钮整条消失**：`Panedwindow` 带 `expand` 却先于底部固定条 pack，窗口回缩时把「后 pack」的底部操作条（刷新历史/获取Cookie）与弹幕开关条挤成 0 高而不可见；改为**最后 pack Panedwindow**，上下两侧固定条先占位，切换弹幕区或缩放窗口后底部控件始终可见
- **弹幕颜色/样式候选改为以服务端可用项为准**：此前用内置候选，账号不支持的颜色/模式发出去会失败；现按所选直播间查询并**按名称去重后如实展示服务端可用项**（只有 1 项属正常，不再回退预设），仅当查询失败/无数据时回退内置预设（避免下拉为空）
- **可用颜色/样式未随直播间同步**：此前只在开启「弹幕」开关与后台就绪时拉取一次，切换直播间、或获取 Cookie 登录后都不会更新；现改为**切换直播间、登录态变化时即时重新拉取**当前房间的可用项（接口按 room_id 查询），并丢弃切换前的过期结果

## [1.6.0] - 2026-09-11

### 新增
- **弹窗常驻开关**：房间列表排序栏新增「弹窗常驻」勾选框（持久化到 `ui.notify_persist`，默认关闭）。勾选后开播悬浮窗不再数秒后自动消失，而是**一直保留直到点击关闭**（卡片内提示「（点击关闭）」），避免人暂时离开时错过开播提醒；悬浮窗总开关关闭时该选项自动置灰（取值保留）

### 优化
- **板块默认占比**：房间列表 / 醒目留言 / 弹幕三区块的默认高度占比调整为 **2:3.5:4.5**（原先由各窗格请求高度决定、占比随内容浮动），窗口尺寸变化时按同比例伸缩；仍可手动拖动分隔条调整（弹幕区隐藏时为 2:3.5）

## [1.5.1] - 2026-09-11

### 优化
- **点击跳转不切换直播间**：房间列表中单击「房间号」或「主播」列打开浏览器时，不再连带把下方 SC/弹幕面板切换到被点击的直播间（列表选中行保持原状）；提醒列开关、按住行拖动排序等交互不变
- **新内容不再强制拉回底部**：SC 区与弹幕区的滚动条不在最底部（正在翻阅历史）时，新到达的 SC / 弹幕静默追加到末尾而不改变视口；仅在原本位于底部时才自动跟随滚动，滚回底部后恢复自动跟随

## [1.5.0] - 2026-09-09

### 新增
- **弹幕显示区**：窗口底部新增「弹幕」开关（最下方，持久化到 `ui.dm_visible`）。开启后在 SC 区下方显示当前选中房间的实时弹幕（时间 + 用户名 + 内容）；**窗口最大化时弹幕区挤占 SC 区空间，窗口化时窗口向下扩展**以不挤压 SC 区，关闭时还原
- **弹幕落盘**：开启期间当前选中房间的弹幕按日写入 `room_<房间号>/dm_YYYYMMDD.jsonl`（缓冲句柄 + 5 秒周期刷盘，与 SC 文件 `sc_` 前缀隔离）；写入失败（文件占用）直接丢弃，不影响显示
- 弹幕仅接收当前选中房间（门控），渲染按轮询周期批量插入，尽量全保留不丢弃；关闭开关或切换房间立即停止接收
- 弹幕区用户名可点击跳转个人空间（从 `DANMU_MSG` 提取用户 uid）
- **表情包弹幕识别**：`dm_type=1` 的表情包弹幕不再显示为空白文本，统一以表情触发词的 `[名称]` 形式显示并落盘
- **三板块可调布局**：房间列表 / 醒目留言 / 弹幕区放入垂直分隔窗格（PanedWindow），拖动分隔条即可调整各板块占用空间
- **SC 删除实时标记**：SC 被删除/退款时直接在原弹幕行尾追加「（已删除，退款）」并整条置灰，不再显示为独立的固定文本行
- 新增 5 个单元测试（DANMU_MSG 解析容错、门控开关、落盘隔离）

## [1.4.0] - 2026-09-09

### 新增
- **GUI 一键获取 Cookie**：房间列表底部新增「获取Cookie」按钮，确认后自动从本机浏览器的 Cookie 数据库读取 bilibili.com 的 Cookie，写入 cookie.txt 并立即应用到当前会话（刷新登录身份）。支持 Edge / Chrome / Brave / Vivaldi / Opera / Firefox（正在运行的浏览器优先，自动选择可用 Cookie，已登录 profile 优先）。新增 `pycryptodome` 依赖（AES-GCM 解密）。限制：新版浏览器运行时独占锁定数据库，需暂时退出对应浏览器；App-Bound 加密 Cookie（v20）暂不支持，失败时明确提示
- **开播悬浮窗提醒**：监听中的直播间从非直播中变为开播时，右下角弹出**自绘悬浮提醒窗**（标题「{主播名} 开播了」，内容为直播标题）。普通置顶窗口实现，**完全不受 Windows 勿扰模式/专注助手影响，无需任何系统设置**，且通过 `WS_EX_NOACTIVATE` 风格保证弹出与点击都**不抢占用户当前窗口的焦点**；自动消失、点击关闭、多条自下而上堆叠（同时最多 4 条）
- **提示音效选择**：排序栏新增「音效」下拉框（上行双音 / 三连音 / Windows 系统提示音 / 静音）与「试听」按钮，选择持久化到 `ui.notify_sound`
- **悬浮窗主开关**：房间列表排序栏新增「悬浮窗通知」全局复选框，持久化到 `gui_rooms.json` 的 `ui.notify_overlay`（默认开启，兼容旧键 `notify_system`）；开播是否提醒仍由每个直播间的「提醒」列独立控制，主开关仅控制悬浮窗这一通道（提示音与任务栏闪烁不受影响）

### 修复
- **开播提示音从未响过**：原先使用 `MessageBeep` 播放系统"星号"音，系统声音方案被设为"无"或对应系统音被禁用时完全无声；改为 `Beep` 直接生成两声上行提示音（后台线程播放），不依赖系统声音方案
- **关播状态不同步**：关播时状态不再卡在「直播中」。处理 `STOP_LIVE_ROOM_LIST`（下播名单含本房间即视为确定性关播信号）；收到 `PREPARING`/下播信号后立即本地置为未开播（不等接口），并在 3 秒后延迟重拉接口复核（失败 5 秒后重试）——规避关播瞬间 `get_info` 接口返回缓存旧状态的问题；确认刷新时若接口仍返回「直播中」但近期收到过关播信号，以弹幕推送为准
- **状态刷新节流改为合并式**：LIVE/PREPARING/ROOM_CHANGE 连发时，节流窗口内的刷新不再被丢弃，而是合并为窗口结束后的一次补发（关播信号紧跟 ROOM_CHANGE 到达时不再被吞）

## [1.3.0] - 2026-09-09

### 新增
- **SC 历史分页加载**：切换房间默认加载最新 100 条，滚动到顶部自动加载更早一页；页边界显示「（已读取 x 条历史记录）」分割点，历史区底部显示「（历史 x 条sc记录）」分割线（固定为切换时已保存的历史总数）
- **开播提醒**：监听中的直播间从非直播中变为开播时播放系统提示音并闪烁任务栏图标（不弹窗）；每个直播间可在列表「提醒」列单独开关，设置持久化
- **同接显示**：标题栏显示直播间实时在线人数（弹幕流 `ONLINE_RANK_COUNT` 消息，监听期间实时更新）
- **舰长数显示**：每次连接时通过 v2 高能榜接口查询（携带主播 uid）
- **直播标题显示**：醒目留言标题栏显示当前直播标题（超 30 字截断）
- **中键快速滚动**：SC 视图与调试日志支持浏览器式自动滚动（按一下中键进入，移动鼠标即翻动，移出窗口仍生效；再按一次或单击左键退出）
- 添加房间支持主播个人空间网址（`space.bilibili.com/<uid>`，自动换算直播间号）；添加成功提示框显示主播 uid

### 修复
- 修复向上翻页时 SC 内容段顺序颠倒、新页被插入到底部的问题
- 修复切换房间后历史 SC 偶发不渲染的问题（Text 组件 disabled 状态下插入无效）
- 同接数据源纠偏：弃用 `get_info` 的 `online` 字段（实为人气值）与 H5 `watched_show`（实为累计"看过"人数）

### 安全
- **CSV 公式注入防护**：以 `=` `+` `-` `@` 或制表符开头的用户名/内容写入 CSV 时前缀单引号（OWASP CSV Injection 防护）
- **解压炸弹防护**：弹幕报文 zlib/brotli 解压输出上限 16MB，超限拒绝并重连

### 变更
- 含房间号/主播名的个人启动脚本（`start_room_*.bat`、`start_sc_*.bat`）移出版本库并加入 `.gitignore`，新增可入库的模板脚本 `start_room_模板.bat`
- README 标记 `--auto` 自动跟随浏览器为实验性功能，建议改用 GUI 启动方式

## [1.2.0] - 2026-09-08

### 新增
- **房间列表排序**：排序按钮手动触发（手动拖动 / 按房间号 / 按主播名 / 按直播状态），可选「直播中置顶」，排序设置持久化；启动时按记忆顺序显示，不再自动排序
- **拖动排序**：按住行上下拖动调整顺序，自动记忆到 `gui_rooms.json`
- **Ctrl 多选批量操作**：批量开启/停用监听、批量删除
- **点击跳转浏览器**：单击房间号打开直播间、单击主播名打开个人空间、单击 SC 用户名打开其个人空间
- 添加房间时记录主播 uid，uid 缺失时后台自动查询

### 修复
- 修复列宽拖宽后超出窗口右侧的问题（松开分隔条自动收紧总列宽）
- 修复个人空间网址中的 uid 被误解析为房间号的问题

## [1.1.0] - 2026-09-08

### 修复
- 修复拖动列分隔条加宽房间号/主播列时内容超出窗口的问题（新增横向滚动条 + 列宽收紧）
- 修复部分直播间直播标题读取不全的问题（合并 H5 接口的完整标题）
- 修复直播状态与标题不实时更新的问题（监听 `LIVE` / `PREPARING` / `ROOM_CHANGE` 弹幕推送，带 2 秒防抖）

### 优化
- 调试日志统一带 `Room[房间号]` 前缀，可区分多房间日志；接入成功与收到 SC 日志补充房间号信息
- 醒目留言区底部常显「（共 x 条SC记录）」计数
- 拖动窗口大小时的卡顿缓解（拖动期间暂停 SC 文本自动换行、日志批量渲染）

## [1.0.0] - 2026-09-08

### 初始版本
- 弹幕 WebSocket 监听：WBI 签名、游客 buvid3、brotli 压缩报文、心跳保活与指数退避重连
- SuperChat 抓取：`SUPER_CHAT_MESSAGE`（含日文翻译版）与删除事件 `SUPER_CHAT_MESSAGE_DELETE`
- 本地存储：JSONL 完整记录 + CSV 关键字段（Excel 友好）；文件被占用自动进入重试队列，重启后恢复补写
- 房间级互斥锁：同一房间不会被多个实例重复监听
- 命令行：多房间监听、`--auto` 自动跟随浏览器（实验性）、`--duration` 定时退出、日志文件
- 图形界面：房间管理（添加/删除/启停/备注）、历史 SC 查看与实时追加、价格着色、调试日志页签
