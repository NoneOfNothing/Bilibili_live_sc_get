- [x] 功能 添加排序按钮，将每次启动自动触发的排序改为手动触发，并添加排序方式选择
- [x] 功能 添加拖动排序直播间功能，添加默认记忆当前排序功能
- [x] 优化 在基础排序的情况上，添加直播中直播间默认置顶开关
- [x] BUG 调整房间号，主播这些宽度时，能超出右侧窗口
- [x] 优化 目前有几个直播间，调试界面就有几个“连接弹幕服务器 wss://zj-cn-live-comet.chat.bilibili.com:2245/sub（第 1 次尝试）”，尝试优化，若优化代价过大，可考虑先不处理
- [x] 功能 点击直播间号跳转到浏览器直播间
- [x] 功能 点击主播名称跳转到主播个人空间
- [x] 功能 输入主播b站个人空间的网址也能添加直播间（不考虑uid添加，因为uid可能与其他人的直播间号相同）
- [x] 优化 调试信息"已接入弹幕服务器，开始监听 SuperChat ..."改为"已接入（房间号）直播间，开始监听 SuperChat ..."
- [x] 优化 主播收到sc时，调试信息显示直播间号
- [x] 优化 sc默认显示100条，滚动条滚到顶时读取前100条，并在截断点显示“（以读取共 x 条历史记录）”，直至全部读取完毕
- [x] 功能 可按住ctrl选中多个直播间，同时开启/关闭监听，删除
- [x] 优化 有“（共 x 条历史记录）”，还需要始终在最下方的“（共 x 条SC记录）”
- [x] 功能 实现浏览器中的按下中键后，滑动鼠标能快速翻动页面的功能
- [x] 功能 监听的直播间开播时提醒
- [x] 功能 实现点击发送sc的用户名字时，跳转到用户个人空间
- [x] 优化 拖动窗口大小时，明显卡顿
- [x] 功能 显示直播间同接和舰长数量（在醒目留言标题栏后面）
- [x] 功能 显示直播间标题（在醒目留言标题栏后面）
- [x] 功能 （可选）显示直播间弹幕，逻辑与sc类似（可能存在压力，要实现需要额外考虑）
- [x] BUG 存在读取直播标题不全的情况
- [x] 优化 直播状态实时更新
- [x] BUG 直播标题未实时更新
- [ ] 优化 升级 Python / Tk 版本验证拖动窗口卡顿是否改善（实测 Tk 8.6.12 每次窗口尺寸变化整体重排约 65ms，与内容多少无关，属 Tk 固有开销）
- [x] 功能 GUI 迁移到 Qt（PySide6），彻底解决窗口拖动卡顿等 Tk 性能限制（重写 gui_app.py 界面层；已按 QT_PORTING.md 完成，业务层 client/api/storage/medal_runner 等直接复用）
- [x] 未验证 监听的直播间开播时提醒
- [x] 未验证 直播状态实时更新
- [x] 未验证 直播标题实时更新
- [x] 优化 退款sc显示
- [x] 优化 开播提醒添加悬浮窗通道及主开关（不受勿扰模式影响、不抢占焦点）
- [x] BUG 直播间关播不同步状态
- [x] 优化 直播列表点击直播间号和主播名称跳转时，不触发切换直播间
- [x] 优化 当滚动条不在最底下时，出现新弹幕和新sc，不会强制把滚动条拉回底下
- [x] 功能 实现通过用户cookie发送弹幕
- [x] 优化 新增勾选框，使开播弹窗长时间保留，需要点击才消失
- [x] 优化 当滚动条不在最底下时，显示新增的sc和新增的弹幕数量提示
- [x] 功能 实现发送表情包（特指直播间专属表情包）
- [x] 优化 断线重连
- [x] 功能 点击弹幕内容自动复制
- [x] 优化 鼠标悬浮在表情包上短时间显示表情包ID
- [x] 优化 将弹幕的表情包显示为表情包图片（实测内嵌图片随历史弹幕量导致卡死，改为悬浮时在提示窗显示原图）
- [x] 优化 表情包显示文本不完全跟随鼠标
- [x] 优化 如果sc区和弹幕区都在最底部，那不管调整区域大小还是打开关闭面板，都保持在最底部
- [x] 优化 取消字数限制
- [x] 优化 取消重复内容检测
- [x] 优化 取消发送确认
- [x] BUG 会卡死
- [x] 优化 点击提醒开关不切换直播间
- [x] 优化 移出仓库的config.json，运行任意脚本自动创建该文件
- [x] 功能 使用cookie获取用户粉丝牌等级（panel 分页拉取，SC 标题栏与「粉丝牌」页签展示）
- [x] 功能 使用cookie获取用户粉丝牌任务（GetActivatedMedalInfo，含观看直播/发弹幕/点赞等，上限随等级、每日刷新）
- [x] 功能 使用cookie自动用户粉丝牌任务中的点赞任务（频繁发包风险；分批 click_time、节流、完成即停、风控即止，需显式开关）
- [x] 功能 使用cookie自动用户粉丝牌任务中的发送弹幕任务（刷屏风险，给出推荐在未开播时使用的提示；默认轮次发专属表情，无专属表情回退纯数字文本，完成即停）
- [x] BUG 自动任务未生效（开播信号直接触发自动点赞，见 55）
- [x] 优化 开播信号直接触发自动点赞任务
- [x] 优化 浏览器扩展获取 Cookie（无需完全退出浏览器：本地回环 127.0.0.1:64321 一次性接收，Origin 校验；原读取浏览器数据库方式保留为备用）
- [x] BUG 老版本 config.json 缺少后来新增的配置项（medal*tasks 等新字段不会自动出现，导致「全自动」总开关看不到、也无从开启；现读取时就地补全缺失键并刷新文件内 *说明，只补缺失项、不覆盖已有值、空对象不填充）
- [x] BUG Qt 版粉丝牌页「仅开播时自动发弹幕」文案说反（该项是附加许可：默认只在未开播时自动发弹幕、勾选后才允许开播时也发）→ 改为「允许开播时自动发弹幕」并补悬浮说明；Tk 版「开播时也自动发弹幕」一并对齐为同一文案
- [x] BUG 自动任务两项互相挤掉（同一房间弹幕+点赞都开时，先提交的发弹幕占住「执行中」，点赞长期不执行）→ 抽出两版共用纯函数 auto_task_types()，把本轮该执行的项汇总成一次提交（engine 本就支持 only=[类型...] 一次依次完成）；未开播不再提交点赞、无粉丝牌房间静默跳过
- [x] BUG Qt 版粉丝牌页「自动」勾选框不随切换房间更新（宿主属性名写错 host.\_medal_emit，异常在槽函数里被吞掉，界面同步整段被跳过，并连带「发弹幕 / 点赞」按钮与自动任务周期失效；已修正为 \_medal_event，并新增 tests/test_qt_wiring.py 静态交叉检查宿主 ↔ 子模块的属性接线）
- [x] 功能 继续迁移qt版本
- [x] 优化 直播状态更新优化（目前无法处理高频开播）：开播推送立即置为直播中并加保护窗口（PUSH_STATUS_TTL），关播信号同样受窗口约束（过期后以接口为准，不再被丢失的推送永久带偏），新增带抖动的周期状态复核兜底，重复开播推送不再重复广播
- [x] 功能 直播预览 · P0 可行性 spike（拉流直链 + Referer 回环代理 + QMediaPlayer；实测 PySide6 6.11.2 / Qt 6.11.2 下 **http-flv 与 hls 均可播放**（1080p H.264 走 MF 硬解、进程 CPU≈1%），故内核定为 **QtMultimedia + 本地代理**；验证脚本 `_spike_live_preview.py` 保留在工作区供复测，结论：需 PySide6≥6.8（FFmpeg 默认后端））
- [x] 功能 直播预览 · P1 单路预览（**Qt 专属**，Tk 无可用视频组件故本轮不做，仅共用框架无关的拉流/代理层）：`api.get_live_stream_urls`（旧接口 `room/v1/Room/playUrl`，web→flv / h5→hls，带 Cookie）+ `live_preview.py` 回环代理（注入 Referer/Origin/Cookie、m3u8 分片改写、直链过期 `set_streams` 换源）+ `qt_preview.py` **独立浮窗**（可缩放/置顶/拖动，播放·停止、清晰度、静音·音量、状态提示；HLS 优先且每 10 分钟自动换源，播放出错自动换另一种格式重试一次）+ 工具栏「预览」入口、房间行右键「预览该房间 / 停止预览」、跟随选中房间、未开播/加密房间/失败给明确提示；**默认自动最高画质**（`preview.quality` 默认 0）、清晰度下拉**按新接口 `getRoomPlayInfo` 的 `accept_qn` 只列该房间可用档位**（旧接口 `accept_quality` 是编号、会误导；不可用则回退到可用最高档并提示后重拉）、**换档先清空 source 再装载**（修掉「换清晰度黑屏」）、浮窗内为**暂停/继续**（定格画面、预览不结束，继续时若断流自动重拉）、**「刷新」按钮与暂停超 30 秒自动跳最新**（直播暂停后继续会滞后），**停止预览（工具栏/右键/关窗）即关窗并回收代理**；**未登录只放 360P/720P 两档并提示登录**（B 站匿名限制）；默认静音、默认置顶，动作按区块打日志）
- [x] 功能 直播预览 · P2 观看时长（`api` 增 `webHeartBeat` 上报：`hb=base64("<next_interval>|<真实房间号>|1|0")` + `pf=web`，按返回 `next_interval` 调度；**仅在预览播放中计时、同一时间只报 1 个房间**；默认关闭、需显式开启且受 `allow_write_operations` 约束，界面显示本次已上报时长，附风控风险提示）—— 实现：接口 `api.report_watch_heartbeat`（GET `live-trace.bilibili.com/xlive/rdata-interface/v1/heartbeat/webHeartBeat`；`heartbeat_hb` / `parse_next_interval` 为纯函数；未登录直接抛 `ApiError`、**不发请求**；返回间隔夹到 15~300 秒，异常回退 60 秒）；浮窗新增「上报观看时长」开关与「已上报 mm:ss」显示；**判定为纯函数 `needs_watch_report`**（预览进行中 + 未暂停 + 主播在播 + 播放器正在播放四者同时满足），暂停 / 缓冲 / 断流 / 未开播都不计、切房即按新房间重新累计（只会有一次预览，故同时只给一个房间计时长）；**默认关闭**（`preview.watch_time`）并叠加 `allow_write_operations` 与登录态双重约束——不满足时勾选被拒并把原因写在开关旁；上报失败（未登录 / 网络 / 风控）立即停表提示、**不自动重试**；风控风险在配置说明与 tooltip 中都写明
- [x] 功能 直播预览 · P3 多路（最多 N 路（默认 2）：仅主路出声、其余静音；卡顿自动降清晰度或停非主路；CPU/带宽上限保护）—— 实现：**上限取 4 路**（`preview.max_rooms`，1~4，硬上限 4）；拆成 `PreviewTile`（一路：自己的播放器/声音/视频组件 + 独立回环代理 + 独立的格式回退与重连预算）与 `PreviewWindow`（编排：宫格布局 `grid_shape`、主路、声音、资源保护）。**主路**：点格子 / 右键「设为主路」切换，只有主路出声（其余强制静音）、主路边框高亮；静音音量暂停刷新作用于主路，清晰度为全局值（各房间按其可用档位落实）。**卡顿保护**：副路只重连 1 次（`HEALTH_MAX_RELOADS_SUB`）后**停路**，主路仍自动重连 3 次；**CPU 保护**：每 30 秒采样进程 CPU，连续两次 >70% 且不止一路时停掉最后加入的副路（`pick_victim_index`）。加入方式：Ctrl 多选批量加入 / 右键单房间 / 每格「✕」停该路；到达上限拒绝并提示。观看时长上报（P2）只对主路；可用清晰度按路缓存，换源不再重复请求
- [x] 功能 直播预览 · P4 打磨（加密房间密码、清晰度记忆、**断流自愈已完成**（随 P1 补入：5 秒心跳判定「在播 + 未暂停 + 播放器停住」→ 自动重新拉流换源，含 15 秒起播宽限期、重试上限 3 次、暂停/未开播豁免；剩余「换源」部分仍靠 10 分钟定时换源）、失败提示文案、README 与测试收口）—— 实现：① **加密房间**：`api.get_stream_info`（新接口的 `encrypted` / `pwd_verified`，纯函数 `parse_room_play_info` 只做透传，未知即 `None`）+ `api.verify_room_pwd` + 两个拉流接口带 `pwd` 参数；加密且未解锁时**不白拉流**，界面提示后由「右键格子 → 输入密码…」输入，密码**仅存内存**（切房/退出即丢，不落盘）；② **清晰度记忆**：写回 `gui_rooms.json` 的 `ui.preview_quality`，启动优先于 `config.json` 默认值；③ **失败提示**：纯函数 `describe_preview_error` 把错误码/异常翻成可操作的中文（未开播/加密/密码错/登录/风控/网络）；④ 收口：新增 13 项测试与 README/CHANGELOG/HANDOFF 同步
- [x] 优化 开播信号打断发自动弹幕任务（执行器新增房间级打断标记：开播信号一到达，正在执行的「自动发弹幕」在下一个检查点停止，等待间隔被提前唤醒、已发出的弹幕不撤回；只打断「自动提交且含发弹幕」，手动按钮与点赞任务不受影响；未勾选「允许开播时自动发弹幕」时才打断，勾选后按用户许可继续。Tk / Qt 两版同步）
- [x] 功能 运行日志落盘，用于事后监测运行状态，受 config.json 控制（新增 logging 段：enabled / level / file / max_bytes / backup_count / categories；抽出 log_setup.py + log_categories.py 供 Tk / Qt / CLI 共用；默认关闭落盘、开启后写入 logs/app.log 并按大小轮转；GUI 勾选调试日志改为相对配置级别的临时提升）
- [x] 优化 Qt 版弹幕流内直接显示表情图，悬浮提示相应改为只显示文字（图未下载完先显示触发词并记下该行，下载完成后原地换图，不影响滚动位置；点击图片同样可复制/右键回复；悬浮只显示触发词/唯一标识，不再重复弹大图）
- [x] 优化 新增「弹幕表情图」开关（Qt 版，窗口底部，状态记忆在界面偏好里，默认开启）：表情图更直观但占高度；**两个方向都作用于已有弹幕**——关闭即把已显示的图片还原为文字（悬浮提示恢复为看原图），重新开启再把弹幕里已有的触发词换回图片（图未缓存则触发下载后自动补），只处理最近约 400 条表情弹幕
- [x] 优化 Qt 版默认窗口宽度改为最小许可宽度（880×900 逻辑像素；宽度对三区块纵向排布没有增益，窄一点反而少留空白）
- [x] 优化 补齐后续改动的日志覆盖：右键菜单三动作与取消（room info/debug）、拖动源改用鼠标按下的行（debug）、拖动到列表边缘自动滚动启停（debug，tick 不记）、点击「提醒」列切换开播提醒（info）；拖动/排序/预览置顶/音频跟随等改动此前已带日志
- [x] 功能 日志覆盖扩展到所有动作 + 分区块启用（区块 room/window/data/task/live/app 可单独开关，默认全开；先看主开关 logging.enabled 决定是否落盘、再看区块开关决定该类是否输出——未标记的第三方日志放行。新增：切换直播间、房间增删启停/排序/拖动/备注/跳转、窗口尺寸与分隔条（节流合并成「首次 → 最后」一条）、页签与面板显隐、接口请求（路径+关键参数+耗时+code）、历史与落盘读写（弹幕按批汇总）、表情包/弹幕选项/粉丝牌刷新、发弹幕与表情、粉丝牌任务（开始/结束/打断）、Cookie 获取、开播提醒、启动退出；工具侧：api 统一请求入口、storage、client、gui*config、room_lock、browser*\*、overlay、medal_runner 全部按区块归类）（新增 logging 段：enabled / level / file / max_bytes / backup_count；抽出 log_setup.py 供 Tk / Qt / CLI 共用，默认关闭、开启后写入 logs/app.log 并按大小轮转，带完整日期与来源前缀；GUI 勾选调试日志改为相对配置级别的临时提升，取消勾选回到配置级别）
- [x] 优化 目前点击不完全显示的直播间时，不管是跳转还是切换，都会自动滚动滚动条使其全显，禁用该功能 —— 实现：Qt 的 `QAbstractItemView.autoScroll`（默认开）会在 current 项变化时 `scrollTo` 把它**完全**显示出来，于是点只露出一半的行、或点横向被裁掉的单元格时视图会自己跳一下。现关闭（`ProtectedLinkTable.setAutoScroll(False)`），只在**拖动排序**（`dragEnterEvent` → `dropEvent` / `dragLeaveEvent`）与**键盘导航**（`keyPressEvent`）期间临时打开——那两种场景确实需要自动滚动，否则拖不到列表边缘、方向键也跟不住
- [x] BUG 预览声音不跟随系统音频输出设备切换（含两次失败修复的结论）—— `QAudioOutput` 创建时选定设备、不会自动跟随系统默认设备。**坑一**：`audioOutputsChanged` 是**实例信号**，写成对类属性 connect 会抛 AttributeError 且被 except 静默吞掉（信号从没接上）→ 改为 `QMediaDevices(self)` 实例连接并保引用。**坑二**：只调 `setDevice()` 播放中常不生效；**播放中换 `QAudioOutput` 实例交给 `setAudioOutput()` 会让播放器卡住（画面定格 + 没声）** → 最终方案：**先停播放器 → `setDevice()` → 先清空 source 再设回同一地址 → `play()`**（`stop()` 后只 `play()` 常常起不来、要等 5 秒健康检查才自愈，所以按「换清晰度」那套重新装载；代理地址本机长期有效，一两秒即回画面，另有 5 秒健康检查兜底）。保留 10 秒巡检与 debug 日志（打印系统默认与各路实际设备）。实测：初始各路＝系统默认；切到另一设备成功且幂等；模拟默认变化后自动跟随回默认
- [x] BUG 预览浮窗置顶的两处问题（取消置顶就消失 / 置顶后右键菜单不显示、主界面像失灵）—— 根因：`setWindowFlag` 会**先隐藏窗口**再要求 show（旧代码把 `isVisible()` 判断放在它之后，恒为 False → 取消置顶即消失）；勾选置顶时重建出的窗口会被系统摆到最前并**激活自己**，而置顶窗口会把主界面弹出的**右键菜单压在下面**——菜单仍抢占鼠标，于是「看不见菜单却在原位点到了菜单项」，随后主界面像失灵。修复：切换前记下可见性与几何 → 切换后按原可见性 show 并恢复位置尺寸 → 浮窗设 `WA_ShowWithoutActivating` 不抢焦点 → **三处弹出菜单自身也置顶**（房间列表 / 弹幕 / 预览格子），`exec` 后压在浮窗之上。**放弃的方案**：Win32 `SetWindowPos` 绕开重建（实测 `WS_EX_TOPMOST` 未真正清掉、且与 Qt 标志不一致）。实测：取消/重新置顶后可见性、位置、尺寸、活动窗口不变；菜单置顶标志生效（对照未设置时不置顶）
- [x] BUG 跳转主页 / 直播间的点击保护失效（自管拖动的回归）—— 根因：改成自管拖动后关掉了 `QDrag`，Qt 默认的「按住移动会按当前索引更新选中」重新生效，按住跳转列稍一移动就改选中。修法：① `selectionCommand` 拦截扩到**按下 / 松开 / 移动**；② 判定用「**按下时的列** 或当前列」（移到别的列时只看当前列会漏）；③ `mouseMoveEvent` 里对「按下点在跳转列」的移动**不交给基类**（实测 Qt 那条路径不经过 `selectionCommand`）。实测：按下 / 同列移动 6px 选中都不变；纯点击照常打开链接；轻微抖动仍算点击；点击非跳转列仍正常切换
- [x] BUG Qt 版右键房间行会切换当前直播间（连带把预览切走）—— 根因：Qt 默认会把选中行改到右键所在行，连带触发「切换直播间」：SC / 弹幕面板被切走，开着「跟随选中房间」时连预览也被切走。现 `selectionCommand` 对右键的**按下与松开**都返回 `NoUpdate`（与跳转列保护同一机制），右键完全不改选中；菜单项按 `indexAt` 定位、与选中无关；「预览该房间」也去掉了 `_select_room`（只加入预览，不切选中）。实测：右键前后选中恒为 `[1]`、选中变化 0 次；左键非跳转列仍正常切换；跳转列保护不受影响
- [x] 优化 拖动时可滚轮滚动列表 —— 原实现借用 Qt 拖放（`InternalMove`），而拖动期间 Qt 进入**平台拖放循环**（Windows 是 OLE `DoDragDrop`），**滚轮事件根本不进 Qt 事件循环**（连应用级事件过滤器都兜不住，属于「做了但没生效」）。现改为**完全自管拖动**：关掉 Qt 拖放，按下 → 移动超过系统阈值进入拖动模式 → 移动 / **滚轮** / 边缘自动滚动（每 50ms 一行）自己处理，插入位置线自绘、`Esc` 可取消，松手按「拖动前快照 + 插入位置」回报主界面重排（`begin_drag` / `update_drag` / `end_drag`）。实测（合成事件走正常分发）：轻微移动不触发；超阈值进入拖动且被拖行 = 按下行；**拖动中滚轮滚动条 `0 → 1` 且插入位置 `5 → 6`（鼠标未动也重算）**；松手提交、`Esc` 取消 0 提交；非拖动滚轮走默认
- [x] BUG 部分情况下拖动会拖走「上一次选中的行」 —— 根因：Qt 按下时通常会把该行设为选中，但**受保护的跳转列**（房间号 / 主播 / 提醒）按下时故意不改选中（「点跳转列不切走 SC 面板」），此时 `startDrag()` 只看选中集合就会拖走旧选中行，落点与拖影也对不上。现以**鼠标按下的行**为准（纯函数 `drag_source_rows`：按下的行有效且不在选中集合里就搬它，否则沿用选中集合整体拖），并把选中切到该行让拖影一致。实测：先选中 111 再按 333 拖动 → 旧逻辑搬 111（BUG）、新逻辑搬 333；多选 `[1,2]` 按其中一行仍整体搬 `[1,2]`；按在列表空白处退回选中集合
- [x] BUG Qt 版手动 / 拖动排序不记忆（重启窗口后顺序被重置）—— 根因：配置文件里 `rooms` 的**顺序**就是列表顺序（启动时 `_room_order = list(entries.keys())`），但 `_save_config()` 直接写 `entries.values()`，而 `entries` 的键顺序从未按显示顺序更新（Tk 版在拖动结束时按 `tree.get_children()` 重排了 `entries`，Qt 版漏了这一步）。现 `_save_config()` 统一按 `_room_order` 重排再写盘（纯函数 `ordered_room_ids`：不在顺序里的补到末尾、已删除的忽略、重复的去掉，两头都不丢房间），拖动 / 手动排序 / 置顶 / 增删房间都会落进配置。实测：拖动后保存并重载 = 拖动后的顺序 `[222,333,444,111]`；手动降序 = `[444,333,222,111]`；**对照旧实现**（不重排直接写）读回仍是原顺序（排序丢失，正是该 BUG）
- [x] BUG 拖动直播间列表排序会导致错位 / 覆盖目标行 —— 根因：`QTableWidget` 继承 `QTableView`，后者 `dragDropOverwriteMode` **默认 true**，放下鼠标不是**插入行**，而是把被拖行的数据从落点**那一列开始**直接覆盖到目标行（拖到行中间就从中间那列往后覆盖）。实测（3 列，首行拖到第 3 行中部）：目标行变成 `['111','主播111','备注333']`（前两列被糊、备注列还是自己的，**行数没变**），即「拖动的行盖在拖到的那一格上、连带覆盖后面的内容」。**只关掉覆盖模式不够**（用户复测仍复现）：`QTableWidget` 的内部移动另有一套「插入/删除 + 清空源单元格」语义，落点不受我们控制。现改为**自管拖放**：`ProtectedLinkTable` 只借用 Qt 的拖动交互与落点提示——`startDrag()` 记录**拖动前的行序快照**，`dropEvent()` 用纯函数 `drop_insert_row` 自己算插入行号（与提示线同一阈值：上边缘/行中间 → 插入该行之前、下边缘 → 之后、列表下方空白 → 末尾），`drag->exec()` 返回后在 `_finish_drag()` 里把「快照 + 源行 + 落点」回报主界面，由 `_on_rows_reordered` 用纯函数 `move_items` 算出新顺序并**重建行**（Qt 糊成什么样都被覆盖掉，且同一帧内完成、看不到中间态）。顺带修掉**拖动后界面比例重置**：重建走 `_populate_rows(fit_height=False)`，不再触发 `_fit_table_height()` → `_apply_pane_ratio()` 按默认占比重置分隔条；重建期间屏蔽选中信号，避免刷掉 SC / 弹幕面板内容。实测：5 种落点（上边缘/行中间/下边缘/末行/空白）算出的插入行号全部符合预期；`move_items` 的单行下移、拖到首尾、多行同移、原地放下、无源行与越界均正确（11 项离线测试）。落点提示改画**行间一条线**（不再框住整行）
- [x] 功能 右键单开一个对应直播间的子窗口，只显示SC区和弹幕区，功能与主程序相同 —— 实现：**Qt 版**抽出视图级组件 `qt_sc_panel.ScPanel`（SC 文本/头部/累计/未读徽标/历史分页/删除置灰/点击跳转，原宿主内联逻辑整体迁入）与 `qt_room_window.RoomChatWindow`（组合 `ScPanel` + `DmPanel`），宿主建立「按 room_id 路由的视图注册表」（`sc_panels_for` / `dm_panels_for` / `room_windows_for`）：`sc` / `delete` 分发给绑定该房间的视图、`dm` 由 `_dm_pending` 改为**按房间分桶**后逐房间渲染、`history` 结果按 `token` 回投发起面板（主界面与各窗口互不判掉对方的结果）、`status` / `online_count` / `guards` 顺带刷新各视图头部与窗口标题；`DmPanel` 的当前房间改为**注入式 `room_provider`**（主视图跟随选中、独立窗口固定房间），独立窗口的弹幕区恒显示且 `_dm_rooms()` 会把它算进弹幕接收门控（主界面「弹幕」开关关着也照收）；**Tk 版**新增 `tk_room_window.RoomChatWindow(tk.Toplevel)`（自己实现一份精简渲染 + 自有小队列读历史，**不消费宿主的 `ui_queue`**），宿主在事件分发处按房间广播 `on_sc` / `on_delete` / `on_dm_batch` / `on_meta_changed` / `on_dm_send_result` / `on_emoticons` / `on_emoticon_image`。两版一致的行为：房间列表**右键「打开独立窗口」**（`Button-3` / `QMenu`，均不改当前选中行）、**一房一窗**（重复打开只前置）、打开时**不抢焦点**（`WA_ShowWithoutActivating` / `overlay.apply_noactivate`）、**不置顶**、按房间记忆几何到 `data/gui_rooms.json` 的 `ui.room_windows`（移动/缩放去抖 1 秒写盘 + 关窗立即写，恢复时 `clamp_window_rect` 收拢到屏幕内）、主窗口关闭统一回收、房间被删除时自动关窗；写操作（发弹幕/表情）沿用 `allow_write_operations` 与登录态门控
- [x] 优化 排序列表的直播中置顶改为一个独立的排序，每次收到开播和关播信号重排，排序根据开播信号的前后排序，取消勾选时，能复原为之前的排序 —— 实现（含后续需求调整）：① **排序栏改造**：去掉「排序」按钮与「直播中置顶」勾选框，**下拉选中即生效**（重排 + 落盘），「手动拖动」改名「**自定义排序**」，排序选择跨重启记忆（`ui.sort_mode`，旧配置的 `pin_live=true` 读取时自动迁移为「按直播状态」）；② **排序方案与显示层分离**：「自定义排序」= 用户拖出来的顺序（**只有它允许拖动**，其它方案下拖动会被忽略——排序栏最右的「ⓘ」会临时变成「⚠」，点开即见「不可拖动、请先切到『自定义排序』」+ 一条 debug 日志），也是**唯一持久化**的顺序（`gui_rooms.json` 的 `rooms`），其它方案只在显示层重排——显示顺序 = 基础顺序 + 方案规则合成（纯函数 `order_room_ids`，Tk / Qt 共用），因此切走再切回「自定义排序」能**原样复原**；③ **「按直播状态」新语义**（原置顶并入其中）：正在直播的按**开播先后倒序**（最近开播在最上）、其后是已下播的按**关播先后倒序**（最近关播的靠前），**轮播中不特殊处理**（与未开播同样归入非直播中、不单独成段）→ 从未开播过的房间落在最后并按自定义顺序；④ **实时重排**：开播 / 关播的状态跳变（弹幕推送与无连接房间的只读查询都算）经纯函数 `update_live_activity` 记录先后标记后立即重排，重建沿用拖动排序收尾那套安全做法（`fit_height=False`、屏蔽选中信号、恢复选中），不会重置分隔条比例或刷掉 SC / 弹幕面板；先后标记不依赖时钟（**实测坑：Windows 上 `time.monotonic()` 精度仅约 15 毫秒**，同一轮事件里几次开播/关播会拿到相同时间戳、排序分不出先后，改为「现有标记最大值 + 1」的自增标记）；⑤ 两版一致（Tk 的拖动禁用落在 `_on_tree_press`、Qt 落在 `ProtectedLinkTable.drag_enabled` / `on_drag_blocked`），新增 19 项离线测试（排序合成、时间记录、迁移、接线）
- [x] 功能 醒目留言头部显示「已播 01:23:45」直播时长（Tk / Qt 两版）—— 仅在「直播中且已拿到起点」时出现，未开播 / 轮播 / 刚启动无数据时该字段消失（头部那一行与没有该功能时完全一致）。**起点口径**：优先接口给的**真实开播时间**（程序在主播开播中途启动也准），取不到再回退「本程序第一次看到它直播中」的观测时刻。实测（2026-09-22，本地 17 个直播间）：`room/v1/Room/get_info` 的 `live_time` 是**北京时间字符串**（直播中 `"2026-09-22 16:35:40"`、未开播 `"0000-00-00 00:00:00"`），H5 `getH5InfoByRoom` 的 `room_info.live_start_time` 是**秒级时间戳**（直播中 `1790066140`、未开播/轮播 `0`），两者换算吻合故**时间戳优先**（`get_full_room_info` 本就在调 H5，零额外请求；字符串按 UTC+8 解析，否则非东八区会整体偏 8 小时）；`getRoomInfoOld` 实测 `code=-400` 不可用已弃。实现：`api.parse_live_started_at`（纯函数，占位 / 0 / 非法 / 未来 2 分钟以上 → None，绝不编造时间）→ `client._emit_status` 随 status 事件下发（**非直播中自动清空**），两版只读查询路径（被占用 / 已停用监听的房间）同样带上 → 两版共用 `update_live_started_at`（接口值可**校正**先到的本地观测；同状态重复上报**不漂移**，否则周期复核会把起点一路推后、时长永远从零开始；下播清空）与 `format_live_duration` / `live_duration_text`。**每秒跳动复用现有 100ms 轮询做 1 秒节流**（Qt `ScPanel.poll_tick` / Tk `_poll_queue`），不新增定时器，只刷「直播中且有起点」的当前房间与各独立窗口；起点与排序用的先后标记 `_live_since`（自增整数）**完全分离**，互不影响
- [x] BUG 「按直播状态」排序重启后被打乱 —— 根因：排序只依赖**运行期内存**里的自增先后标记，退出即丢，重启后回落到自定义顺序；且记的是「本程序观察到跳变的时刻」而非真实开播时间，即便存下来也不准。现改为**真实墙钟时间 + 持久化**：写进 `gui_rooms.json` 的 `ui.live_started_at` / `ui.live_offline_at`，启动读回（`prune_live_marks` 丢弃超过 24 小时 / 未来 / 非法的记录）。开播时刻直接复用「已播」时长的起点（接口真实开播时间，H5 `live_start_time` 时间戳优先，`get_info` 的北京时间字符串回退），因此**启动时就已在直播的房间等于用已播时长反推开播时间**，接口一到即校正。新增纯函数 `update_offline_at`（只认「直播中 → 非直播中」的真实跳变，避免重复上报推后关播时间、也不给从未开播的房间留记录；同一轮撞值用「现有最大值 + 1 毫秒」保证仍能排序）与 `gui_config.parse_room_time_map`；旧的运行期标记 `_live_since` 整体弃用，排序时 `live_since` 直接传 `live_started_at`。两版在时间基准变化时立即落盘、删房时清理。实测（假 self 调两版真实 `_save_config` 走「运行 → 落盘 → 重启」）：顺序重启前后一致 `[11,33,22,44]`；26 小时前 / 未来 / 非法记录被丢弃
- [x] BUG 重启后「记忆了排序方案，列表却仍是自定义顺序」—— 根因：Tk 版启动填充固定按**配置顺序**（＝自定义顺序），`_populate_rows` 还留着「不再启动时自动排序」的旧注释（排序靠手动按钮时代的做法）；Qt 版启动虽算了显示顺序，但状态陆续到达时若「时间基准没变」也不重排，开局仍像自定义。现：① Tk 启动按 `_sorted_room_ids()`（当前方案的顺序）填充；② **状态跳变**（不限时间基准变化）即触发重排，启动后各房间状态陆续到达时在播房间会被排到最前；③ 重排**合并执行**——事件里只置 `_sort_dirty` 并记 debug，由本轮 100ms 轮询末尾统一 `_apply_sort()` 一次（避免启动时 N 个房间逐个重排、Qt 版反复重建整表），真正重排时记 info
- [x] BUG 进行部分房间的点赞任务时，显示已达储蓄亲密度上限，但b站网页端和手机端自行点击都能完成任务 —— 根因：粉丝牌执行引擎 `_complete_locked` 拉到任务信息后，只要接口字段 `reach_free_intimacy_limit`（该粉丝灯牌刚点亮）为真，就**整房跳过**并回「已达储蓄亲密度上限：投喂一个粉丝灯牌即可领取，暂不执行」，该房间的点赞 / 发弹幕一条都不执行。该字段表示**该粉丝灯牌刚点亮**——此时点赞 30 次 / 发弹幕 10 条这类**免费任务暂时拿不到亲密度**（官方规则第 7 条明文「熄灭状态下……点赞行为和发送弹幕行为**仅点亮勋章不可获得亲密度**」；用户已确认 3044248 / 5050 / 1016 三个勋章都是**靠点赞 30 次重新点亮**的；正常点亮状态下照给亲密度，实测 1700301235 今日 +20）。本次已实测：接口 data 里确实返回 `free_intimacy` / `reach_free_intimacy_limit`，20 个房间均有值；官方规则页只讲「每日上限」，**没有**「储蓄亲密度」一说；且它是**按勋章（每个直播间）各自累计与判定**的——同账号下 `free_intimacy` 出现 0 / 12 / 100 并存、触顶标记也逐房间不同，故不能用某个房间触顶外推其它房间，这正是「部分房间」的来源。上限口径实测：面板 `day_limit`（每日亲密度上限）在 level 2~28 的 25 个勋章上**全是 20000**（＝官方「粉丝团成员每日 2W」，不随等级变），而免费额度触顶点为 **100**；且该标记**与房间内任务进度无关**——1700301235 的点赞 / 弹幕任务全完成（10/10）仍返回 `free_intimacy=0 / reach=false`，3044248 三个免费任务几乎没做（0/10、0/10、4/10）却返回 `100 / true`；且实测 `light_source=1` 的 4 个勋章 `free_intimacy` 全 >0（100/100/100/12）、`light_source=0` 的一律 0 / false（`free_intimacy` 数值本身的语义仍未确证，不影响本节结论）。该标记只影响亲密度是否继续累加，**与任务能否完成无关**——点赞 / 发弹幕仍会照常计入任务进度（网页端、手机端手动点能完成即证）；但**它绝不表示任务不该做**：长时间不做任务粉丝灯牌会熄灭，做任务正是为了点亮并维持灯牌。现改为**任务照常执行**、只把缘由说明出来：命中时记一条 `task` info 日志（「粉丝灯牌刚点亮：免费任务暂不增加亲密度，任务照常执行以点亮并维持灯牌」），并在结果说明末尾附「（粉丝灯牌刚点亮，点赞 / 发弹幕暂不涨亲密度，任务仅用于点亮并维持灯牌）」；此外补上**灯牌熄灭**（接口把任务下发为「仅点亮」、上限 0）的情形：原先 `is_task_applicable` 判其「当前不适用」直接跳过，导致灯牌熄了就再也点不亮；现新增纯函数 `is_light_up_task`（未完成且上限 ≤ 0）并纳入 `pending_write_tasks`，按**官方点亮任务次数**执行（点赞 30 次 / 发弹幕 10 条，常量 `LIGHT_UP_LIKE_CLICKS` / `LIGHT_UP_DANMAKU_COUNT`）——点亮阶段接口不下发进度，故不按「进度推进」判定，发满即收尾（点赞发一次后复核一次，复核仍为「仅点亮」则提示「已发起点赞（30 次）用于重新点亮」）。任务已完成即显示「已完成」，服务端确实不推进时仍走原有「点赞进度未推进（x/y）」上报（连续无进展达重试上限即停，不空发）。两版共用同一引擎，Tk / Qt 同步生效；新增 / 改写离线回归测试 5 项（该标记为真时点赞 `click_time=30` 与发弹幕 2 条照常执行且附缘由说明、任务已完成时同样附说明、未命中时说明保持原样、上限 0 时按点亮次数发满点赞 30 次与弹幕 10 条、`is_light_up_task` 与 `pending_write_tasks` 的判定）
- [x] 功能 新增快捷弹幕功能 —— 为**每个直播间**维护一组快捷弹幕文本：发送行内提供下拉，**点选后只填入输入框、不直接发送**（可先改后发，避免误触）；主界面另有「管理」按钮，可在界面内增删 / 上移下移（**关闭即保存**）。**按房间独立**——存在 `gui_rooms.json` 房间条目的 `quick_danmaku` 字段（`RoomEntry` 新增，读入由纯函数 `normalize_quick_danmaku` 清洗：只留非空字符串、去首尾空白、超长按 20 字截断、保序去重、上限 12 条；旧配置缺该键自动为空）。填入规则为纯函数 `merge_quick_danmaku`：空则直接填入、非空则**追加**（不覆盖已输入内容）、已恰好相同则不重复。Tk 主界面 / Tk 独立窗口 / Qt（主界面与独立窗口共用 `DmPanel`）三处全部生效；下拉放在**发送行内**故不新增行高，Qt 侧 `DmPanel` 新增 `show_quick_manage` 参数、管理入口只在主界面。实测（Tk 启动 + 自建带预设的房间）：有预设时下拉 `('打卡','好耶')` 且 `readonly`、无预设时 `disabled`；空输入框点选得 `打卡`、非空点选得 `打卡 好耶`（追加不覆盖、字数同步为 5）；管理对话框添加 / 删除后关闭，落盘 `['好耶','新快捷语']` 正确。新增 16 项离线测试（`tests/test_quick_danmaku.py`，刻意不依赖 PySide6 以便无 Qt 环境也能跑）。**后续按反馈修正两处观感**：① Qt 版下拉原先用 `placeholderText("快捷弹幕")`，而 Qt 的占位文字按**灰色**绘制、看起来像「控件被禁用」（用户反馈「有选项时仍为灰色」）——现两版统一改为把「快捷弹幕」做成下拉的**第一个真实项**（常量 `QUICK_DM_PLACEHOLDER`，按正常颜色显示，选中它不做任何事），Tk 侧同步由此获得了占位文本（此前无值时下拉是空白，用户反馈「按钮上没文本」）；② 实测（Tk 启动 + 自建房间）：有预设时 `values = ('快捷弹幕','打卡','好耶')` 且 `readonly`、无预设时 `values = ('快捷弹幕',)` 且 `disabled`、两者显示文本都是「快捷弹幕」，选「打卡」得 `打卡` 且下拉复位为占位、选占位项输入框不变；③ **Qt「管理」按钮点击无反应已修**——根因是 `qt_app.py` 漏 import `QDialog` / `QListWidget`（补 import 的那次编辑被同批次另一编辑覆盖），`open_quick_danmaku_manager` 一进入即 `NameError`，而 Qt 槽函数内的异常在 `pythonw` 下无控制台可见，表现为「按下没反应」。**同时纠正一个长期验证盲区**：依赖装在项目内 `.venv`，此前用系统 Python 跑测试会静默跳过 `test_gui_support` / `test_qt_wiring` / `test_qt_preview` 等 Qt 测试（只跑 313 项）；改用 `.venv\Scripts\python.exe` 后 **524 项全部通过**。Qt 侧实测（.venv，9 项）：点「管理」成功弹窗（修复前 0 个）、增删后内存与落盘一致；④ **日志按惯例补齐四处**：填入（`window` info，三处各一条）、打开管理对话框（`window` info，Qt 另有房间不存在时的 warning）、对话框内增删 / 上下移（`window` debug）、保存（`task` info，仅内容变化时记）——实测日志已落盘（`gui_qt.dm.window: 快捷弹幕：填入「打卡」（房间 1，仅填入不发送）`）
- [x] 优化 首页底部的弹幕开关与操作按钮各占一行，在弹幕区下方留出一片空白 —— 与 Qt 版底部布局对齐：把「显示弹幕区」开关与「刷新历史 / 获取Cookie / 从插件获取」三个按钮合并到**同一行**（Tk 版原先 `dm_bar` 与 `bottom` 两个 Frame 合为一个，仍 `pack(side="bottom")` 紧贴窗口底部），勾选框文案由「弹幕」改为与 Qt 一致的「显示弹幕区」。实测（空房间目录启动后读取控件坐标）：页签内底部只剩**一个** Frame（y=627、h=27），5 个控件同在 y=0~3 的一行（x 依次 0 / 89 / 331 / 424 / 517，总宽 604 < 984），底部到页签底仅 6px 边距，弹幕区（TPanedwindow）下方不再留空行；Qt 版布局本就如此，未改动。**真正的空白来源另在发送栏下方**：那里有两行提示 Label（`dm_send_hint_var` 发送结果 / `dm_copy_hint_var` 已复制），**无内容时仍各占一行高度**（共 42px），空着看过去就是一片空白（历史遗留）。现改为**空提示不占位**：新增 `_sync_dm_hint_rows()` 随文本变化 pack / `pack_forget`（每次按「发送提示 → 复制提示」固定顺序整体重排，避免复制提示先出现时顺序颠倒），Qt 版对应新增 `_set_hint()` / `_set_copy_hint()`（空文本 `setVisible(False)`，构造时初始隐藏）。实测（空房间启动读控件坐标）：两条提示都为空时 `dm_send_area` 高 69 → **27**、`dm_frame` 需求高 206 → **164**（正好少两行）；启动期的真实提示（如「后台初始化中，请稍候…」）照常占位（48px）；两行同时出现时 `send_y=27 < copy_y=48` 顺序正确
- [ ] BUG 打开程序时电脑桌面图标短暂「穿透」窗口闪烁一两下 —— 用户 2026-09-26 报告，**未指明 Tk 还是 Qt 版**。本次排查（Tk 版）：启动时窗口在 `_dm_adjust_window` 给弹幕区加高（实测日志 `1498x1138 → 1498x1366`）的阶段**窗口尚未映射**——用 `winfo_ismapped()` 实测「旧流程」与「先 `withdraw` 再 `deiconify`」两种写法在调尺寸时同为 0，即尺寸调整本就发生在显示之前，故 `withdraw/deiconify` 方案实测无差异、**已回退**。现象未复现，怀疑与「窗口尺寸接近屏幕高度（可能触发全屏优化）」或 DPI 缩放后的窗口尺寸有关，留待复现后再查
